{
  "name": "cts-kilo-db-bootstrap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "db:migrate": "bun -e \"console.log('[db:migrate] Provisioning bootstrap has no schema migrations.')\"",
    "dev": "bun run server.js"
  },
  "dependencies": {
    "@kilocode/app-builder-db": "file:vendor/app-builder-db-marker"
  }
}
