{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "app/api/**": {
      "maxDuration": 30
    },
    "app/api/cron/**": {
      "maxDuration": 60
    },
    "app/api/engine/**": {
      "maxDuration": 60
    },
    "app/api/connections/*/progression/**": {
      "maxDuration": 45
    }
  },
  "installCommand": "corepack enable && corepack prepare pnpm@10.28.1 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.28.1 --activate && pnpm run vercel-build"
}
