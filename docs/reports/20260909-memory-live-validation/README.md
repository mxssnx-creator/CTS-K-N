# CTS-K-N: Speicher, Live-Admission und Remote-Abnahme, 9. September 2026

report.html ist der vollständige eigenständige Diagrammbericht. data.json enthält die bereinigten Messdaten, die 11 Remote-Monitoring-Stichproben und die Herkunft einzelner Prüfungen. remote-monitor-10m.json enthält dieselbe sanitisierte 10-Minuten-Messreihe separat. Der ausgewiesene Exchange-/Server-Messstand für die vorherige Installation ist PR334 / ab113322; der neue QuickStart-/Connector-Stand wird erst nach seinem grünen Merge und dem anschließenden offiziellen Reinstall als aktiv bestätigt.

Regeneration: python3 generate.py [AUSGABEVERZEICHNIS] mit Python, matplotlib und lxml. Keine Börsen- oder Netzwerkzugriffe. Die mitgelieferten Messdaten werden gelesen; SVG-IDs und Grafik-Metadaten können beim erneuten Rendern abweichen. Für originale Messergebnisse die eingecheckte JSON-Datei verwenden.

Der aktuelle lokale Quellstand besteht den isolierten Volltest mit 304 Suiten / 2.124 Tests / Exit 0; TypeScript und ESLint sind ebenfalls grün. Die letzte isolierte Serverkopie vor diesem UI-Patch bestand 303 Suiten / 2.121 Tests / Exit 0. Drei VST25-Versuche sind fehlgeschlagen; die anschließende reine GET-Nachprüfung bestätigt eigene Restmengen und offene eigene Testorders jeweils null. Die zwei vollständig abgerechneten letzten Zyklen ergeben netto −0,018365 virtuelle USDT. Keine Bestätigung positiver Profitabilität oder vollständiger Produktionsreife. Die Konfigurations-CoverageMatrix ist ein Fixture-Nachweis; echte Börsenzyklen und Abrechnungen sind separat ausgewiesen.

Die 10-Minuten-Messreihe umfasst 11 lesende Stichproben in 60-Sekunden-Abständen. Alle sechs Remote-APIs waren HTTP 200, alle drei Dienste blieben aktiv mit NRestarts=0, Redis war erreichbar und die verbindungsspezifische Live-Admission blieb requested=true, enabled=true, executionMode=blocked, blockCode=entry_protection_halt. Die Exchange-Gesamtpositionssicht wechselte währenddessen von 0 auf 1; ausgeschlossene/nicht zuordenbare Positionen und Orders bleiben separat ausgewiesen. Das ist kein Beleg für eine CTS-eigene neue Entry-Ausführung.

Die regelmäßigen Log-Grenzen und geprüfte Bereinigung sind dokumentiert. Brutto freigegebene Dateibytes werden von aktuellem freien Dateisystemplatz unterschieden. UI-Browser-Abnahme, ein neuer unabhängiger 14-Tage-Holdout für die native Trend-Evaluation und Klärung des fortbestehenden Entry-Schutzstopps bleiben offen.
