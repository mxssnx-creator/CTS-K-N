# CTS v3.7 – Projektinformation und Fortsetzung

## Current continuation — 2026-09-12: overall controls and empty-book recovery

This entry supersedes stale workspace/revision/pending statements below. Canonical checkout: `/workspace/CTS-K-N`; branch `codex/overall-control-orders-20260912`; source baseline and verified GitHub main `36fab4aa86c932e1550206023b58ed432e1a9d43`. Recovered from verified source bundle under `/workspace/backups/CTS-K-N/recovery-20260912`. No newer published main or open PR was found during this continuation; prior PR346 readiness delta is included. Do not restore obsolete worktree paths.

Uncommitted release delta: default-off Overall Control Orders only across global/nested/connection settings and presets; canonical false overrides legacy/global true. Shared SL/TP uses the outermost owned ranges and exact physical slot quantity; individual strategy exits remain active. Modes switch only after cancellation/fill confirmation. Durable original-quantity allocations recover shared partial fills without replaying quantities; cumulative PnL/fee corrections are applied by difference. Statistics and dialogs expose shared references/counts. Stable shared cycles perform no redundant position writes; concurrent protection-policy reads are deduplicated. All policy-enabled Main Sets already dispatch without a hidden per-symbol budget; operator quality/capacity/fresh-signal limits remain authoritative.

Remote read-only baseline on 2026-09-12: `/opt/cts-kn` remains `36fab4aa`; cts-kn, cts-kn-scheduler, cts-kn-direct-trade active; HTTP3002 health200. X02 signed reads confirm 0 positions/0 orders, both snapshots ok=true. No remote mutation in this continuation. Existing halt reason is `entry_protection_rollback_unconfirmed`. Found/fixed source defect: empty local books returned before the existing two-authoritative-snapshot halt reconciliation. Unknown/simulated snapshot evidence cannot clear it. Direct status now overlays the runtime halt, matching canonical Live. Remote proof is pending deployment; never manually delete the halt.

Before the recovery fix: 304 suites/2119 tests plus production build (42 static pages,350 traces), typecheck/lint/security passed. Including recovery: 304 suites/2121 tests and TypeScript passed; final production build passed (42 static pages,350 traces), lint/source-syntax/security (1725 files,0 findings), preflight (37 checks,schema108), recreation verification (1717 files), and nine local HTTP page checks passed. Browser download was unavailable; visual/hydration interaction verification remains open. Publication/deployment are pending until subsequent evidence. Checkpoints verified with SHA-256 and git bundle verify: `/workspace/backups/CTS-K-N/20260912T181207Z-before-overall-controls` and `/workspace/backups/CTS-K-N/20260912T195235Z-before-empty-book-recovery`.

User explicitly authorizes push/merge and live-enabled X02 Prod-VST reinstall. Publish only reviewed green main; take fresh checkpoints before publication/merge/deploy. X01/Mainnet and Bybit stay read-only. Minimum virtual volume, exact ownership, cleanup/baseline restoration still apply to lifecycle tests. Existing historical ledger lineage/accounting gaps and pruned lifetime contributions are not repaired or claimed complete. Remaining work: release gates/UI, publish/merge, official reinstall, complete engine/dispatch/partial-fill/statistics monitoring, post-deploy confirmation of the fourteen-day DCA diagnostic, final backups and exact handoff. The diagnostic used gap-free public BCH/XRP/SOL 5/15/30-minute candles from 2026-08-29 inclusive to 2026-09-12 exclusive: 15552 configurations,46656 runs including two same-period diagnostic folds,2591387 trade observations; quantity/PnL/time invariants passed,319 baseline/47 stress-qualified variants. This is not independent validation, exchange execution or a basis for default changes; full-grid interactive HTML and reproducibility evidence are separate user artifacts. Never claim all valid historical configurations are immediate market entry signals or claim complete live acceptance without evidence.


<!-- CTS_LATEST_START -->
## Maßgeblicher Fortsetzungsstand — 2026-09-09, QuickStart-Live-Anzeige und Remote-Monitoring

Dieser Block hat Vorrang vor älteren Pending-/Revisionsangaben darunter. Canonical /workspace/CTS-K-N bleibt mit den gesicherten 34 älteren Änderungen auf 2041787b erhalten. Die aktuelle Integration liegt im verknüpften Worktree /workspace/CTS-K-N-worktrees/vst25-live-results-20260908 auf Branch codex/fractional-close-grid-20260909; keine fremden Änderungen resetten. Jede ältere Datei ist in docs/LEGACY-INTEGRATION-20260909.md zugeordnet.

PR332–334 sind grün gemergt; der zuletzt offiziell installierte Serverstand ist ab1133226744e93864f4ad51f26152cd920a259e mit 350 Build-Traces. Der aktuelle lokale Stand ergänzt die begrenzten Connector-Logs und korrigiert die QuickStart-Live-Anzeige: der Schalter zeigt dauerhaft den angeforderten Operator-Intent, während die verbindungsspezifische engine-states-Admission separat executionMode, effective, blockCode und blockReason liest. Historische liveDispatchBlocked/liveDispatchFailed-Zähler können dadurch keinen gesunden aktuellen Zustand mehr als blockiert markieren. Ein sichtbarer Hinweis erklärt einen bestehenden Entry-Protection-Halt.

Der QuickStart-Testdialog enthält jetzt einen eigenen Schritt Live Entry Admission. Er liest genau dieselbe Connection-Readiness, zeigt requested/effective/mode/block als Zusammenfassung und markiert einen absichtlichen Schutzstopp als Warnung. Der allgemeine Infrastruktur-Health-Check kann diese Warnung nicht mehr als vollständigen Live-Erfolg verdecken; der Gesamtbericht wird bei Warnungen PARTIAL. Die bestehenden Wartungs-/Regressionserwartungen wurden auf diese Semantik aktualisiert.

Lokale isolierte Vollprüfung nach diesen Änderungen: 304 Suiten / 2124 Tests / Exit0 in 48,57 s; TypeScript, ESLint und git diff --check bestanden. Die letzte isolierte Serverkopie vor diesem UI-/Dialog-Patch bestand 303 Suiten / 2121 Tests / Exit0 in 80,734 s. Tests liefen mit leerer Umgebung und scripts/test-network-isolation.cjs; keine unisolierten Tests mit Exchange-/Redis-Zugangsdaten.

Das abgeschlossene lesende Remote-Monitoring für BingX X02 lief 11 Stichproben im 60-Sekunden-Abstand von 03:48:31 bis 03:58:41 UTC. Alle sechs APIs waren in jeder Stichprobe HTTP200; cts-kn, cts-kn-direct-trade und cts-kn-scheduler blieben active mit NRestarts0; Redis war erreichbar. Live blieb requested=true und enabled=true, aber executionMode=blocked mit blockCode=entry_protection_halt. Eigene offene Orders blieben 0. Die Exchange-Gesamtpositionssicht wechselte zwischen 0 und 1; ausgeschlossene/nicht zuordenbare Zähler wurden getrennt gemessen, daher ist dies kein Beleg für eine neue CTS-eigene Entry-Ausführung. Es wurde keine Schutzsperre gelöscht und kein neuer Orderlauf gestartet.

Der eigenständige Diagrammbericht liegt in docs/reports/20260909-memory-live-validation/report.html, data.json und remote-monitor-10m.json; generate.py regeneriert ihn offline. Er enthält die Speicher-/AOF-Diagramme, die native Trend-Coverage, die tatsächlichen VST-Abrechnungen und das 10-Minuten-Redis-/Positionsdiagramm. Drei VST25-Versuche bleiben nicht vollständig bestanden; die zwei vollständig abgerechneten letzten Zyklen ergeben netto -0,018365 virtuelle USDT. Es gibt keinen unabhängigen neuen 14-Tage-Holdout für die erweiterte native Trend-Matrix, keinen Browser-Abnahmepass (der Statistik-Endpunkt war vom Browser-Werkzeug blockiert) und keinen Nachweis dauerhafter positiver Profitabilität. Keine Produktionsreife oder garantierte Gewinne behaupten, solange diese Gates offen sind.

Die zuvor abgeschlossene Übergrößen-/AOF-/Redis-Bereinigung bleibt gültig: nur verifizierte ungenutzte historische Caches und nie ausgeführte X02-Fehlversuche wurden entfernt; aktive Daten, Zugangsdaten und Backups blieben erhalten. Die automatische Freigabeprüfung lehnte ein vollständiges Projektarchiv ab; dieses Archiv wird nicht erneut übertragen. Für Remote-Prüfungen wurden nur bereits geprüfte, eng begrenzte Dateien beziehungsweise der offizielle gemergte Installationsweg verwendet.

Vor dem nächsten Push/Merge/Reinstall einen neuen owner-only Checkpoint erstellen. Danach ausschließlich den grünen GitHub-main-Commit über den offiziellen Installer ausrollen, remote erneut API-/Service-/Redis-read-only prüfen und den exakten Merge-/Rollback-/Reinstall-Beleg hier ergänzen. X01/Mainnet/Bybit und fremde Positionen/Orders bleiben lesend beziehungsweise unangetastet.
<!-- CTS_LATEST_END -->


## Verbindliche Fortsetzung und Integration (Nutzeranweisung 2026-09-06)

- Bei jeder Fortsetzung alle älteren unveröffentlichten Änderungen, lokalen Branches, offenen PRs und neueren Änderungen inventarisieren und anhand des gemeinsamen Vorgängers vergleichen.
- Jede ältere Änderung entweder integrieren, als bereits enthalten belegen oder ihre Überarbeitung durch eine neuere Implementierung einzeln dokumentieren. Keine ältere Arbeit stillschweigend verwerfen oder neuere Korrekturen mit älterem Code überschreiben.
- Verifizierte Backups erstellen; danach integrieren/überarbeiten, vollständig testen, über GitHub pushen und grüne PRs mergen; erst anschließend gemergtes main remote reinstallieren und HTTP/UI, Statistiken, Koordination und Stabilität erneut prüfen.
- Canonical Workspace, verwalteten Chisel-Zugang, persistente Konfiguration und Daten über Reinstall und Chatwechsel erhalten.
- Historische Tests umfassen 14 vollständig abgeschlossene UTC-Tage. Konfigurationsdimensionen, vollständige ausgeführte Matrix, nicht abgedeckte Dimensionen, Datenlücken, Kosten, PF, Drawdown und Ergebnisse pro Konfiguration im HTML-Bericht ausweisen.
- Systemweite Defaults/Mindestwerte nur nach belastbaren positiven Ergebnissen nach Kosten und unabhängiger Validierung ändern. Mehrere hundert Orders sind ein Kapazitäts-/Koordinationsziel, kein Grund, Qualitäts-, Schutz- oder Eigentumsprüfungen zu umgehen. Exchange-Abnahmetests bleiben X02 Prod-VST mit virtuellem Mindestvolumen; fremde Orders unverändert lassen.
- Keine Produktionsreife behaupten, solange erforderliche Gates, Remote-Stabilität oder Order-/Statistikabgleich offen sind. Exakte Fortsetzung in `docs/PROJECT-CONTINUATION.md` und `.kilocode/rules/memory-bank/context.md` pflegen.

Aktive Integration: `/workspace/CTS-K-N-worktrees/continuation-20260906`, Branch `codex/continuation-20260906`, Basis `752d4e5a155094978ee647ed323173edca74bf98`. Canonical Altstand bleibt unter `/workspace/CTS-K-N` bis zum geprüften Abschluss gesichert.

Älterer Stand: `2041787b`, 31 geänderte und 3 damals unversionierte Dateien; sämtliche 34 Dateien einzeln per Dreiwegevergleich geprüft. 13 identisch, 4 bereits integriert, 17 Konfliktdateien vollständig durch neuere Implementierungen abgedeckt. Konflikte betreffen additive Block-Counts/Stufen, Margin- und Order-Schutz, Retention, Basket-Generation und Test-/UI-Verträge. Der neuere additive Block-Vertrag bleibt erhalten. Keine alte Funktion ist durch blindes Überschreiben entfernt worden.

Offener Dashboard-Stand aus PR315 wurde in den Integrationsbranch übernommen. Der bestätigte Vercel-Fehler ist eine fehlende `vercel-build`-Kompatibilitätsalias; Integration benutzt exakt denselben Build sowie identische Vor-/Nachbereitung wie `build`. Ein Statistikfehler markiert fehlerfreie Zyklen ohne Signale als fehlgeschlagen; Integration korrigiert diesen anhand der tatsächlichen Pipeline-Fehler. Bestehende historische Fehlerzähler bleiben als Altdaten erhalten.

Remote-Vorabprüfung 2026-09-06: verwalteter SSH-Banner bestätigt, Host `v2202607384858486523`, `/opt/cts-kn` auf `752d4e5a`, HTTP200, CTS-K-N-Dienste aktiv/NRestarts0; Redis PONG, NRestarts63, Speicher wieder ca.6,7GiB. Vollständige aktuelle Abnahme noch offen. X01-Zyklen sind nicht allein wegen fehlender Signale fehlgeschlagen.

Checkpoint: `/workspace/backups/CTS-K-N/20260906T102515Z-continuation`; Bundle und sämtliche SHA256-Prüfungen bestanden.

- Zusätzliche Nutzerpriorität: DCA zuerst für ein Symbol (aktuell BCH), danach XRP/SOL über jeweils 14 vollständige Tage rechnen. Alle ausführbaren Stufen 1–4 und mehrere letzte SL-Abstände/Mulitplikatoren vergleichen. Geringen Drawdown vor höherem Gewinn priorisieren, positive Ergebnisse nach Kosten und Kostenstress getrennt ausweisen. Bereits betrachtete Daten nicht erneut als unabhängigen Holdout bezeichnen. Vollständige Ergebnisse mit Diagrammen als interaktives HTML liefern.


## Verifizierte Fortsetzung 2026-09-06, PR320

- PR320 integriert ältere Arbeiten, PR315 und main/PR319 (`b3d4e9f5`). Canonical und parallele Worktrees bleiben erhalten; kein Reset fremder Änderungen.
- Historie: 2026-08-23T00:00Z bis 2026-09-06T00:00Z exklusiv, BCH zuerst, danach XRP/SOL, neun lückenfreie Reihen. Rohdaten-SHA256 `eb9fb582b1936fdceae2ac221c43a7361119a853a1d495e31de531e068b10916`.
- DCA: 217.728 Erstläufe + 1.037.610 deduplizierte SL/Stufen-Läufe = 1.255.338 Konfigurationen. 179 strenge Kandidaten vollständig unter doppelten Kosten geprüft; BCH 108/0, XRP 5/0, SOL 66/4 (Kandidaten/Stress bestanden). Priorität niedriger maximaler Drawdown, danach Exposition und Nettoergebnis. Wiederverwendetes Fenster ist retrospektiv, kein unabhängiger neuer Holdout. Kein universeller Default qualifiziert oder live übernommen.
- Direct-Trade: 90.048 vollständige Konfigurationen, 151 gültige, sieben Strategietypen. Matrixvalidität allein qualifiziert keinen Livebetrieb.
- Persistierte Ergebnisse: `CTS-v3.7-Diagramme-und-Kandidaten.html`, `CTS-v3.7-14-Tage-Ergebnisse.html` und `CTS-v3.7-14-Tage-Rohdaten.zip`. Vollständiges HTML enthält alle DCA-Konfigurationen und Direct-Trade-Blockauswertungen, Diagramme, Filter, Details und CSV. Alle 22 Archivdateien per SHA256 geprüft. Bericht zeigt Strategie-Notionalpunkte, keine Kontorendite; Funding nicht enthalten.
- Neue Redis-Korrektur: Staging-Chunks EX1800, Verlängerung nur beim Lease-Inhaber, Veröffentlichung prüft atomar Eigentümer und Vollständigkeit, persistiert aktuelle Chunks und setzt alte auf EX300 für laufende Leser. Fehler/Abbruch und verlorene Veröffentlichung-Antwort schützen die aktuelle Generation. Kompaktierung verwendet ebenfalls vollständigen atomaren Manifestwechsel. Fortschrittsanzeigen sind Lease-geschützt.
- `scripts/cleanup-direct-trade-orphans.ts --connection-id ID` ist standardmäßig lesend, begrenzte SCAN-Seiten mit fortsetzbarem Cursor. `--apply` entfernt nur persistente, eindeutig alte, nicht aktuelle Chunks bei fehlender Berechnungs-Lease; Lease/Manifest/TTL werden atomar erneut geprüft. Andere Connections und Orders sind nicht im Schlüsselbereich. Erst nach Backup und Installation der Korrektur gegen Produktionsdaten anwenden.
- Portable Installer-Tests verwenden den tatsächlich ausführenden Testbenutzer, eindeutige Instanznamen und isolierte temporäre State-/Backup-Pfade; Fake-SSH/Sudo/Systemctl bleiben reine Fixture-Befehle ohne Hostrechte.
- Vollständige Regression lokal und in separater Serverkopie: 290 Suiten / 2.002 Tests bestanden, Exit0; externe HTTP-Origin- und Proxy-Verbindungen gesperrt. Native isolierte Redis-Verifikation auf dem Server: 14/14 Sicherheits- und Lebenszyklusfälle bestanden, inklusive Lease-Rennen und verlorener Commit-Antwort. Produktionsdienste und Exchange-Zugangsdaten wurden dafür nicht benutzt.
- Verifiziertes Remote-Rollback vor PR320: `/var/backups/cts-kn/20260906T113109Z-pre-pr320-reinstall`, Quellbundle, Laufzeitarchiv, private Konfiguration, fork-frei validierte native AOF-Kette und SHA256-Prüfungen. Remote aktuell PR319, Reinstall von PR320 erst nach grünem Merge.
- Offen bleiben tatsächliche Abnahme nach Reinstall, Langzeitbegrenzung des Redis-Wachstums, geladener Overview-/Stage-Dialog und ein strenger koordinierter X02-Prod-VST-Order-Soak. Frühere fremde Baseline-Änderung bleibt ein fehlgeschlagener Soak. Keine vollständige Produktionsreife oder mehrere hundert erfolgreich abgeglichene Exchange-Orders behaupten.


### Ergänzung: 32-Symbol-VST-Abnahme und X01 (2026-09-06)

Nutzer verlangt Push/Merge/Reinstall, erneuten VST-Lauf mit 32 Symbolen und funktionierende X01. `BINGX_VST_SOAK_SYMBOL_COUNT=32`, `BINGX_VST_SOAK_CYCLES=32`, `BINGX_VST_SOAK_DURATION_MS=2400000`, `BINGX_VST_SOAK_ALLOW_SHORT=1` und `BINGX_VST_SOAK_ENGINE_TRAILING_UPDATE=1` planen 32 vollständige Abläufe mit 75 Sekunden Abstand und 256 Venue-Submissions. Dies ist ein Plan, kein Ausführungsnachweis. Kandidaten kommen ausschließlich aus dem aktuellen Prod-VST-Vertragskatalog, werden gegen vorhandene Exposition, Spread und das unveränderte virtuelle Mindestvolumen/25-USDT-Gesamtnotional-Limit geprüft. Wenn weniger als 32 ausführbar sind, wird die Abnahme blockiert; keine stille Reduktion.

Der VST-Runner verlangt weiterhin einen echten Wartungsmarker und inaktive CTS-K-N-Dienste. Ein rein lesender Vorversuch vor Wartung wurde erwartungsgemäß vor dem ersten Exchange-Request blockiert. Schutz nicht umgehen. Nach dem Reinstall geordnet in Wartung wechseln, VST durchführen, alle eigenen Orders/Positionen und die fremde Baseline abgleichen, anschließend Dienste wiederherstellen und prüfen.

BingX X01 im Dashboard ist `bingx-8581b0cb8581`, nicht der nicht vorhandene Alias `bingx-x01`; die ältere nicht zugeordnete Dublette `bingx-90fb3a5490fb` bleibt unverändert. X01-Verbindungstest über den bestehenden Connector bestand (HTTP200, SDK, prod-live), ausschließlich Kontolesen ohne Order. Verarbeitung meldet laufende Zyklen; historische/realtime-Indikationsabdeckung wird separat geprüft. Mainnet und alle Bybit-Verbindungen bleiben lesend, Live-Allowlist bleibt auf X02 begrenzt.

## 2026-09-06: Live/overview continuation (PR320, not yet deployed)

User additionally requests continuous cycles, only Base-added and Base-enabled connections in the server overview, regular bounded memory reclamation, more native Trend tuples, and X02 virtual exchange acceptance over 32 symbols. Preserve these requirements in subsequent chats.

Implemented: primary cycle metrics now expose every processing cycle; productive cycles remain separately named. Empty historical completion markers now require measured candles and intervals before reuse. Overview connections require both Base flags, and global cycle aggregation reads scoped progression before legacy fields. QuickStart separates latest qualified Real rows, current active sets, blocked entries and entry failures; observed zero positions remain zero. Direct-Trade owner sync performs at most 20 SCAN pages per connection every five minutes under a shared Redis cadence; old orphan deletion retains atomic generation/lease/TTL checks. New staging/current-generation TTL rules remain in PR320.

Remote evidence: installed main still b3d4e9f (PR319); X02 tick count advanced from the screenshot's 2 to 7. Memory pressure reached 238 MiB available / 14.1 GiB swap in use; other project processes are untouched. X02 dispatch records show budgets below executable minima, missing venue tickers, and DCA waiting for confirmed parents. A separate GET-only authenticated VST connector test subsequently obtained valid BTC/SOL/BCH/XRP tickers (4/4). No mainnet order is authorized. Do not increase risk limits or invent fills to make Live green. Current orders/positions remain unverified by the pending 32-symbol VST soak. Historical 14-day reports remain reproducible and persisted; do not call retrospective refinements an independent holdout.

Validation before final UI/memory additions: 292 suites / 2,013 isolated tests passed. Final checks, GitHub publication/merge, official reinstall and actual 32-symbol/32-cycle/40-minute virtual-minimum soak remain pending. Canonical dirty changes and the continuation worktree must never be reset to align API-generated commit history.

Final verification update: 293 suites / 2,016 tests passed with external networking blocked, including real-M1 hydration, matrix/count parity and bounded memory cadence. GET-only VST live-market audit obtained 1,441 real M1 candles for each BTC/SOL/BCH/XRP; the native Trend matrix emitted signals on SOL/BCH/XRP. These are observed indication signals, not forward-performance qualifications or executed orders. Runtime now hydrates a missing M1 day through the same connection's connector; failed venue history attempts back off five minutes independently from the 30-second Redis retry. Backup /var/backups/cts-kn/20260906T132410Z-pr320-final verified; CTS-K-N was placed in official maintenance at approximately 13:32 UTC because Redis reached its memory ceiling, before reinstall/soak. Other project services remain untouched. Do not leave maintenance unnoticed: complete merged-green reinstall and owned-order reconciliation, then restart with service-control.sh start, or record a precise recovery blocker.

## Abschlussstatus PR322/PR323 und Remote-Abnahme — 2026-09-06

- PR322 ist als `0862ed419a47d763eea43bd443101286e19292a7` und PR323 als `ef29134264048e71dc73d3cd98813c2acb70d3c7` in `main` gemergt. Die offizielle Remote-Installation liegt exakt auf `ef29134264048e71dc73d3cd98813c2acb70d3c7`; alle drei CTS-K-N-Dienste sind aktiv, der Wartungsmarker ist gelöscht, `/api/health` meldet `healthy` und Redis `connected`. Der verifizierte Reinstall-Backupstand ist `/var/backups/cts/cts-kn/20260906T154303Z`; der vorherige vollständige Release-Backup `/var/backups/cts-kn/20260906T132410Z-pr320-final/VERIFIED` bleibt gültig.
- Der Live/Overview-Fix projiziert die kanonische Readiness auch in die Legacy-Aliase: X02 meldet `running`, `execution_mode=live`, `live_execution_mode=live`, `live_trade_requested=1`, `live_trade_enabled=1`, `canPlaceRealOrders=true`, gültige Zugangsdaten und dauerhafte Koordination ohne Blockcode. Die Serverübersicht hat 1 aktive/aktivierte Verbindung von 13; X02 ist die einzige aktivierte Exchange-Verbindung. X01 (`bingx-8581b0cb8581`) wurde separat lesend getestet (`success=true`, `prod-live`, HTTP-Connector, keine Order); Mainnet/Bybit bleiben schreibgeschützt.
- Nach dem Reinstall bestätigte die echte Rotation `configuredSymbolCount=32`, `attemptedCurrentTick=32`, `succeededCurrentTick=32`, `coveredUnique=32`, `complete=true`, `failedSymbols=[]`, `stalledSymbols=[]`. Die Realtime-Zähler sind dadurch nicht mehr bei einem künstlichen `1` eingefroren: der erste neue Zyklus wurde beobachtet und der nächste lief weiter. Die aktuelle Overview-Quelle meldete dabei 32 aktive Symbole, 46.24 Mio. kumulative Indikationen, 5 Indikations-/Strategiezyklen, 15 kanonische Live-Positionen und 5.000 behaltene geschlossene Zeilen; die native Trend-Klasse wurde separat als reale Indikationsklasse gezählt (zum Messzeitpunkt 6.060).
- Die 293 Suites/2.016 Tests, TypeScript, ESLint, der optimierte Produktionsbuild (349 vollständige Traces), Dev-Preview-Smoke und die Vercel-Deployment-Prüfung waren grün. Der Build verwendet die installierte begrenzte Heap-Konfiguration; der 32-Symbol-VST-Loader lief mit `--max-old-space-size=1536` und blieb bei rund 194 MB Peak-RAM.
- Der authentifizierte X02-Prod-VST-Lauf verwendete virtuelle VST-Mittel und das echte Mindestvolumen. Plan: 32 Symbole, 32 Zyklen, 75 s Abstand, 256 geplante Venue-Submissions, native Trend-/Signal-/DCA-/Block-/Trailing-Abdeckung. Ausgeführt wurden 3 vollständige Zyklen (BTCUSDT direct, JSTUSDT main, COREUSDT preset) mit 25 Order-Submissions und 403 Requests; 0 Requests wurden geblockt. Entry, Akkumulation und Close füllten in den ersten beiden Zyklen, inklusive Trailing-Proof. Der Lauf wurde im dritten Zyklus geschlossen, weil während des gemeinsam genutzten Kontolaufs eine fremde COREUSDT-Position (`venue quantity 0.1`, eigene Menge `0`) erschien. Der Schutz verweigerte jede Mutation fremder Exposure; `success=false` ist deshalb ein sicherer Abnahmeabbruch, kein Live-Bypass. Cleanup ist vollständig (`cleanupComplete=true`, eigene Rest-Exposure `0`, eigene Control-Orders `0`), die fremde Baseline blieb erhalten. Ein erneuter Voll-Soak darf nur mit weiterhin leerer/ausgeschlossener geteilter Symbol-Baseline erfolgen; Limits oder Schutzprüfungen werden nicht erhöht.
  - Damit ist X02-Prod-VST guarded live dauerhaft aktiviert und nachweislich venue-authentifiziert; eine Freigabe für echtes Mainnet oder ein allgemeiner systemweiter Live-Default folgt daraus nicht.

## 2026-09-07: X02 Redis rejection-storm guard

- PR #326 was merged on 2026-09-07T18:51:33Z as merge commit `8d0f1f38ed0a21ab38faa01f0b6f23acb80e7a8e` on `main`, after all required checks passed (`smoke`, `Vercel – cts-k-n`, `Vercel – cts-v`, and preview comments). `/vercel/share/v0-project` and the recovered canonical `/workspace/CTS-K-N` now point to merged `main` with clean worktrees; the canonical remote was restored to GitHub. The feature branch remains locally retained for provenance, while its remote branch was deleted by the merge. No unrelated PR315 work was merged or reused.
- Root cause addressed: BingX position reads could turn a 109429/100410 account-snapshot failure into repeated empty reads while Live dispatch created a unique zero-fill rejected row for each retry. Position reads now share the BingX cooldown lane, use account-scoped short-lived cache/single-flight, preserve unhealthy status/retry hints, and fail closed during cooldown.
- Live persistence now suppresses pre-entry zero-fill `rejected`/`error` placeholders without an exchange handle, removes dedup-lock collision persistence, and uses a bounded connection-scoped entry-halt marker after an unauthoritative position snapshot. Filled, exchange-owned, pending-fill, and recovery rows remain durable; Direct-Trade control records are unchanged.
- Validation: 289 unit suites / 1,955 tests, 4 integration suites / 72 tests, and full Jest 294 suites / 2,028 tests passed; TypeScript, ESLint, source syntax, production build (349 traces), frozen pnpm install, release secret scan (1,686 files / 0 findings), and recreation-manifest verification (1,678 files) passed. The deployment-contract check is intentionally unresolved because this local VM has no deployment URL; no remote endpoint was assumed.
- Focused coverage now includes recovery after the 109429 cooldown expires, in addition to no replay during cooldown, REST fallback, account-cache isolation, transient-row cleanup, and entry-halt dispatch.
- Owner-only checkpoints verified with bundle/SHA-256 manifests: `/workspace/backups/CTS-K-N/20260907T180031Z-x02-rejection-storm-fix`, `/workspace/backups/CTS-K-N/20260907T181758Z-x02-rejection-storm-fix-implemented`, `/workspace/backups/CTS-K-N/20260907T182831Z-x02-rejection-storm-fix-docs`, `/workspace/backups/CTS-K-N/20260907T183514Z-x02-rejection-storm-fix-recovery-test`, `/workspace/backups/CTS-K-N/20260907T185240Z-post-pr326-merged-main-sync`, and `/workspace/backups/CTS-K-N/20260907T185416Z-merged-main-handoff-docs`; SHA-256 manifests and `git bundle verify` passed.
- Publication gate passed through PR #326. The merged-main runtime gate is blocked: required `/workspace/.network-clients/activate-cts.sh` is absent and no managed Chisel listener/SSH configuration is available in this VM. Per the repository safety rules, no ad-hoc tunnel was substituted. No remote reinstall, service, order, Redis, or exchange mutation was made; remote health, row growth, statistics, and owned-order reconciliation remain unverified. X01/Mainnet/Bybit remain read-only, and no production-readiness claim is made.

## 2026-09-08: remote update attempt

- PR #328 was merged on 2026-09-08T14:46:17Z as merge commit `ac1d7fb4a750a3cb0ad0789ccf8e8bc9eaed07cf`; smoke and both Vercel deployment checks passed, and the remote feature branch was deleted. PR #329 then merged on 2026-09-08T15:27:03Z as `7110bc07dbee78ab139f6e7e8de72de92b557feb`, also with all required checks green. The canonical `/workspace/CTS-K-N` checkout is clean on `main` at `7110bc07`; GitHub `main` matches exactly. `/vercel/share/v0-project` remains a clean local provenance branch at `09de81a`, whose content is contained in merged `main`; open PR #315 remains conflicting/dirty and unrelated, and was not merged or reused.
- Created owner-only verified source checkpoints `/workspace/backups/CTS-K-N/20260908T143100Z-pre-remote-update` (manifest SHA-256 `ffe84358c9d61355c96621a6d662336a371df964b8bc40f34a9c27f0507f8fac`), `/workspace/backups/CTS-K-N/20260908T144711Z-post-pr328-merged-main` (manifest SHA-256 `b3d82986430c38a5121353a127950f2c5b56e5f9e149847213b48dbcaa672eb8`), and `/workspace/backups/CTS-K-N/20260908T152734Z-post-pr329-merged-main` (manifest SHA-256 `9069629d0eb559325df3d65db2b8f3ef361271148317be81fbbf48b2775bbd8b`). Each includes bundles, binary worktree patches, untracked-file records/archives, HEAD/status/ref records, `git bundle verify`, and `sha256sum -c` results.
- Remote update remains blocked before mutation: `/workspace/.network-clients/activate-cts.sh`, `/workspace/.network-clients`, `/workspace/.ssh`, and the pinned localhost SSH forward are absent. The repository's managed-access rule forbids substituting the local helper or an ad-hoc tunnel. Source push/merge is complete through PR #329, but no remote reinstall, service, Redis, order, or exchange mutation was performed; remote runtime health, row growth, statistics, and owned-order reconciliation remain unverified.


## 2026-09-09: VST25, Live-Ergebnisse, Redis und begrenzte Logs

Aktive Nutzeranforderungen gelten über Chatwechsel: neuestes main plus ältere lokale Änderungen einzeln prüfen; getestete Korrekturen über PR pushen/mergen; nur grünes gemergtes main offiziell remote reinstallieren; UI/Stats/Redis/Speicher prüfen und gemessenen HTML-Bericht liefern. Aktuell X02 Prod-VST mit 25 Symbolen und Mindest-PF 1,3. Live bleibt als gewünschte Einstellung aktiviert; tatsächliche Ausführung bleibt von Schutz-, Eigentums-, Budget- und Kontosnapshot-Prüfungen abhängig. X01/Mainnet/Bybit bleiben lesend. 50–500 Orders sind ein Abnahmeziel, positive Ergebnisse keine Zusicherung.

- Kanonischer älterer Checkout `/workspace/CTS-K-N` ist tatsächlich auf `2041787b` mit älterem uncommitted Stand wiederhergestellt; ältere Notizen über einen dort sauberen main beschreiben eine frühere VM. Nichts dort resetten. Neue Arbeit im verknüpften Worktree `/workspace/CTS-K-N-worktrees/vst25-live-results-20260908`, Branch `codex/vst25-live-results-20260908`, Basis `1aaba49f`. Dateiweise Auflösung: `docs/LEGACY-INTEGRATION-20260909.md`.
- Neue Settings: `liveConfigAutoDeactivateEnabled=true`, `liveConfigLossWindow=12` (5–25, Schritt 1). Nur vollständig abgeschlossene, ausgeführte und durch Exchange-Settlement belegte Live-Positionen zählen, inklusive Gebühren. Exakte Symbol/Richtung/Intent/Set-Identität; atomare idempotente letzte 25 Ergebnisse, dauerhafte Deaktivierung bei negativem Netto-Summenfenster. Keine Rückdatierung historischer Trades ohne belegte Set-Zuordnung. Deaktivierte Konfigurationen erscheinen paginiert in Statistics. Bestehende Positionen behalten Schutz und Recovery.
- Native Trend: zusätzliche EMA-Paare und Bestätigungen. Standardmatrix über vier Timeframes: Trend 72 Auswertungen / 144 gerichtete Sets. Neue Kombinationen erhalten eindeutige IDs, Legacy-Basis bleibt erhalten. Kein als optimal oder profitabel zertifizierter neuer Default.
- Live-Mindestvolumen korrigiert nur für eindeutig bestätigtes X02 Prod-VST: gerundetes Venue-Minimum innerhalb des unveränderten 0,1%-Kontobudgets. Ungenügende Budgets bleiben gesperrt. Historische leere Fehler-Intents zählen nicht länger als geöffnete/geschlossene ausgeführte Positionen. Live-Orderzähler verwenden jeweils einen vollständigen Connection-Ledger, inklusive verfügbarer expliziter Null; historische Zähler bleiben erhalten und sind als kumulativ bezeichnet.
- Live-Summary Fehler werden als HTTP503/unavailable geliefert. Client behält datierten letzten Erfolg, meldet Fehler, dedupliziert Anfragen und bricht bei Connection-Wechsel/20s ab. Übersicht berücksichtigt zusätzlich echte Runtime-Entry-Halts, ohne die persistierte Live-Anforderung auszuschalten.
- Redis: gezielte X02-Retirement-Regel nur für mindestens eine Stunde alte `rejected/error`-Records mit Null-Ausführung, ohne Exchange-IDs, Fills, Controls oder offene Mutationen. Hash und Spiegel werden atomar vollständig verglichen, dann auf 60s Ablauf gesetzt. Jede gleichzeitige Änderung verhindert Bereinigung. Andere Connections und ausgeführte Positionen bleiben außerhalb dieser Regel. `scripts/retire-unsubmitted-live-records.mjs` ist standardmäßig lesend; Apply benötigt verifizierten Server-Backup, Wartungsmarker und alle drei Dienste inaktiv. Native isolierte Redis-Verifikation: 14 Outcome- und 8 Retirement-Prüfungen erfolgreich; keine Produktionsschlüssel benutzt.
- Logs: maximal 1000 System-Archiv-IDs samt Entfernung ausgeschiedener Payloads, 1000 ausstehende kompakte Records; Metadaten werden vor dem Puffern auf begrenzte Tiefe/Elemente und gültiges JSON <=8192 Zeichen verkleinert. Progression hält maximal vier gleichzeitige Redis-Flushes, eine Schreiboperation je Connection, kein Timeout-Replay und keinen überlappenden globalen Flush. Log-API liest je Quelle höchstens 100 Records, bündelt gleiche Aufrufe für 5s und führt keine vollständigen Keyspace-Scans aus. Nicht gemessene Key-Zahlen sind unavailable; aus Redis-MB abgeleitete falsche Zykluszeiten entfernt. Disk-Logs bleiben durch vorhandenen 5-Minuten-Timer begrenzt; Journals ca.256 MB.
- Remote auf `/opt/cts-kn`, verwalteter Chisel aktiv, offiziellen Reinstall von `1aaba49f` durchgeführt. Alle drei CTS-K-N-Dienste aktiv, health healthy. X02 25er-Basket/PF1,3 und Live=true gespeichert und gelesen. Messung vor Legacy-Retirement: Redis 4.049.305.040 Bytes used / 4.209.061.888 RSS, 1.037.826 Keys. Stichprobe alter Live-Fehler: 160/160 ohne Ausführung oder Exchange-Handle. Aktuell belegter Schutzstopp `entry_protection_rollback_unconfirmed`; eigene Venue-Positionen 0, eigene Control-Orders 3, fremde Baseline 3 Positionen / 6 Orders. Keine Freigabe durch blindes Löschen dieses Stops.
- Übergrößenbereinigung ausgeführt: 30 Dateien aus fünf ungenutzten historischen Webpack-Caches unter `/var/tmp/cts-kn-*`, 5.330.003.422 Bytes freigegeben. Prozessbezug und Symlinks geprüft, vorher Dateigrößen/SHA256 in owner-only Bericht dokumentiert; Quellcode, laufender Build und Ergebnisse behalten.
- Backup lokal vor Logs: `/workspace/backups/CTS-K-N/20260909T001051Z-pre-log-overload-fix`. Server vollständig verifiziert: `/var/backups/cts-kn/20260909T0030-pre-redis-release/VERIFIED` (tatsächliche Verifikation 00:21:55 UTC), plus vorherige `20260908T2310-pre-vst25-main-reinstall` und `20260908T2326-pre-vst25-settings`. Nur diese verifizierten Pfade als Backup-Nachweis verwenden.
- UI: vorhandener HTTPS-Origin lädt die App. Overview-Dialog im Browser blieb wegen Fetch-/Zugriffssperre unvollständig. Browserumgebung hat direkten Zugriff auf einen API-Pfad ausdrücklich blockiert; keine Umgehung. Unabhängige Serverprüfung: Stats HTTP200/101263 Bytes/30ms, Logs HTTP200/162389 Bytes/2626ms, Health HTTP200/13ms. Das ist kein vollständiger Browser-Abnahmepass.
- Vor Veröffentlichung dieser Erweiterungen: finale 300 Suiten / 2101 Tests mit detectOpenHandles bestanden; TypeScript ohne Fehler. Veröffentlichung/Merge des neuen Branches, erneuter offizieller Reinstall, produktive Retirement-Bereinigung, Behebung des belegten Runtime-Schutzstopps und vollständiger koordinierter VST25-Soak stehen noch aus. Keine Produktionsreife behaupten. Historische 14-Tage-Reports aus vorheriger Fortsetzung bleiben retrospektiv; keine neue native Trend-14-Tage-Abnahme behaupten.

Neue parallel gemergte Dokumentation PR331 (`8262e4cc67b48c089c720058d071cbbdd570b181`) vor Veröffentlichung integriert. Sie ändert nur Kontext/Manifeste; getesteter Laufzeitcode ist identisch zur ursprünglichen Basis. Neuer Release-Parent ist PR331.


### Nachtrag zum Testabschluss (2026-09-09)

PR332 ist mit Commit `7c025d1d1c7fa355584e31ac1c8dcc4e2250c30c` gepusht, aber noch nicht gemergt. Smoke und beide Vercel-Deployments sind grün. Die vollständigen Tests hatten zunächst 300/2101 erfolgreiche Assertions, aber einen verspäteten Log-Timer aus einer bereits beendeten Test-VM und dadurch Exit1. Globale Fixture-Bereinigung in `__tests__/diagnostic-cleanup.ts` beendet jetzt den Progression-Timer, verwirft verbleibende Test-Puffer und wartet auf bereits laufende Test-Flushes. Produktions-Logging und dessen Fehlerstufe bleiben unverändert.

Die automatische Freigabeprüfung stoppte außerdem einen unisolierten lokalen Gesamttest wegen möglicher unerwarteter BingX-Zugriffe. Keine Umgehung: Die vorhandene `scripts/test-network-isolation.cjs` wurde geprüft und mit explizit abgewiesenen HTTP-/Socket-Verbindungen verifiziert; der anschließende Lauf erfolgte mit leerer Umgebung, ohne lokale .env-Zugangsdaten und mit diesem Schutz auch für Node-Kindprozesse. Ergebnis: lokal 300 Suiten / 2101 Tests / Exit0 (65,858s); isolierte Serverkopie mit bereinigter Umgebung ebenfalls 300 / 2101 / Exit0 (76,596s). TypeScript und ESLint der Testergänzung sind fehlerfrei. Der Produktionscode bleibt identisch zum mit 350 vollständigen Traces geprüften Build. Testergänzung und aktualisierte Manifeste werden als Folgecommit derselben PR veröffentlicht; erforderliche GitHub-Prüfungen anschließend erneut abwarten.

Remote-Leseprobe 00:32:17 UTC: frischer unbeschränkter X02-Prod-VST-Positionssnapshot `ok=true`, 0 Venue-Positionen und 0 offene Orders; keine Exchange-Mutation ausgeführt. Das frühere App-Summary mit 3/6 fremden Restzeilen ist damit kein aktueller Venue-Bestand. Der persistierte Entry-Protection-Halt bleibt gesetzt. Der aktive lokale Index enthält 8 IDs, davon ein Pending-BCH-Intent ohne Exchange-Handle/Ausführung; erst nach aktueller vollständiger Reconciliation und atomarer Prüfung einen Halt freigeben. Nicht allein wegen dieses früheren leeren Snapshots löschen.

### Remote release and memory recovery — 2026-09-09 01:40 UTC

PR #332 merged as `fdd33ed14b135a5c7808463e3ae5bb336af1ccb4`; smoke on merged main passed. The official bootstrap reinstall completed on `/opt/cts-kn` with 350 validated build traces; all three services were active with NRestarts=0. Installer rollback: `/var/backups/cts/cts-kn/20260909T011708Z`. Local pre-merge checkpoint: `/workspace/backups/CTS-K-N/20260909T011340Z-pre-pr332-final-merge`.

The guarded native Redis retirement completed with Exit0: 354954 representations scanned, 328885 eligible at read, 241870 retirement operations, 87015 preserved at write (including an already-shortened peer TTL). Before: 536432 keys / 3992661648 used bytes / 4057042944 RSS. After expiration and purge: 52624 keys / 773217360 used bytes / 1366818816 RSS. All three sampled executed lifecycle hashes remained byte-identical. The prior 30-file unused-build-cache cleanup reclaimed 5330003422 bytes. Native BGREWRITEAOF subsequently reduced AOF from 10892367760 to 467857440 bytes, reclaiming 10424510320 bytes; the logical key count stayed 51152 and write/rewrite status remained OK. Backups and active runtime binaries were retained.

Independent server health/status/logs/settings/live-summary requests returned HTTP200 after retirement. Status-all now correctly reports exactly the one enabled X02 engine, 25 symbols, requested Live=true and canPlaceRealOrders=false due to `entry_protection_halt`. Old lifetime counters (including 300691 failures) were not reset or represented as new errors. Full browser overview acceptance remains open because the browser tool explicitly blocked its stats URL; do not bypass that URL through another browser/transport. An unrelated pending command lost its network-approval response; it was not a passing API test. Later checks excluded the blocked stats route.

The remote settings GET exposed stale flattened Live-OFF fields while the canonical versioned connection was Live-ON. A follow-up GET-only projection now uses the canonical Live switch/request while retaining a separate runtime admission check. It preserves explicit canonical OFF, even with a stale ON alias; three actual route tests prove ON/OFF/requested distinctions and no Redis writes. Checkpoint before this edit: `/workspace/backups/CTS-K-N/20260909T013515Z-pre-live-settings-projection-fix`. This follow-up is not deployed until its own green merge.

A strict X02 Prod-VST soak is running from an isolated archive of fdd33ed1: 25 requested symbols, 25 complete paced cycles, 2400000 ms, up to 25 USD virtual position notional and 200 planned cumulative venue submissions. Results are pending; this is not 200 simultaneous open orders or a profitability backtest. Runtime services are temporarily stopped by the existing maintenance wrapper and restored in its EXIT trap. No foreign order or mainnet mutation is authorised. The old halt may be recovered only after successful cleanup plus fresh empty-book audits with canonical admission checks and Redis WATCH/lease fencing. Operator scripts/reports: `/var/tmp/cts-kn-vst25-20260908`; run unit `cts-kn-pr332-vst25-soak`. Do not claim the soak, halt recovery or positive strategy results passed until the saved final evidence proves it.

### First VST25 attempt and causal fix — 2026-09-09 01:45 UTC

The first fdd33ed1 VST25 attempt stopped after the first BTC lifecycle: 8 actual venue order submissions, entry and accumulation filled, row/security controls and engine trailing replacements observed, and the reduce-only close fully filled 0.0002 BTC. The next quantity check reused the connector's pre-close one-second cache (last fresh positions request at 01:36:51.432, close submission at 01:36:51.934, settlement read at 01:36:52.532); it incorrectly treated the cached 0.0002 as excess exposure versus expected zero. Cleanup finished with zero owned exposure/control residuals and the initial empty account baseline restored; all three services restarted. This is a failed 25-symbol acceptance, not a completed cycle or a proven external position. The old protection halt was deliberately left intact.

Follow-up code adds a bounded fresh-position reader: cache/shared-inflight or pre-audit snapshots are not used for quantity/protection decisions; provider errors remain blocking, and the connector's normal cache expiry/request pacing is retained. The VST exact-quantity check and production connection-wide protection audit now use it. The strict excess-quantity/direction/ownership checks are unchanged. Tests cover cached pre-close exposure, cached zero hiding a new entry, unavailable provider snapshots and stale-read timeout. Checkpoint `/workspace/backups/CTS-K-N/20260909T014137Z-pre-fresh-position-audit-fix`. Publish/green merge and reinstall this fix before rerunning authenticated VST; do not edit the installed source or use an unmerged harness.


## Aktueller verifizierter Stand — 2026-09-09 02:24 UTC

Diese Ergänzung ersetzt die vorherigen Pending-Angaben zu PR332/333. PR332 wurde als `fdd33ed14b135a5c7808463e3ae5bb336af1ccb4` gemergt. PR333 mit der kanonischen Live-Settings-Projektion und frischen Positionssnapshots wurde nach grünem Smoke und beiden Vercel-Prüfungen als `f1bb1be97de3dd525db1afe156074937c8fa5bce` gemergt und offiziell remote reinstalliert. Build: 350 Traces; alle drei CTS-K-N-Dienste danach aktiv, keine Neustarts. Vollständige isolierte Tests lokal 302 Suiten / 2109 Tests / Exit0 (49,521s), Server 302 / 2109 / Exit0 (78,231s), TypeScript und ESLint bestanden. Keine weiteren Änderungen am kanonischen schmutzigen Checkout `2041787b`.

Backup vor Reinstall: `/var/backups/cts-kn/20260909T0155-pre-pr333-reinstall/VERIFIED`; offizielles Rollback `/var/backups/cts/cts-kn/20260909T020303Z`; lokaler Dokumentationscheckpoint `/workspace/backups/CTS-K-N/20260909T022151Z-pre-final-validation-report`. Gesamtfreigabe durch alte Caches und AOF-Komprimierung: 15.754.513.742 Dateibytes (Bruttosumme). Nach erneutem Reinstall Redis 790.459.568 Bytes used / 1.347.383.296 RSS / 51.609 Keys.

Der zweite VST25-Lauf läuft unter `cts-kn-pr333-vst25-soak-r2`, Berichtswurzel `/var/tmp/cts-kn-vst25-20260908/vst25-r2`, Quellstand exakt `f1bb1be9`. Zuletzt acht vollständige Zyklen bestätigt, jeweils flat=true. Dies ist noch kein abgeschlossener 25-Zyklus-Nachweis. Die drei normalen CTS-K-N-Dienste sind währenddessen absichtlich inaktiv; der EXIT-Wrapper stellt sie wieder her. Erfolgreicher vollständiger Soak und Cleanup sind Voraussetzung für den anschließenden streng lesenden VST-Empty-Book-Audit; nur bei zwei frischen leeren Venue-Snapshots und atomarer Ledger-/Lease-/Halt-Prüfung darf der bekannte alte Halt entfernt werden. Schutzprüfungen niemals umgehen.

Beide kanonisch priorisierten X02-Settings-Hashes enthalten Base/Main/Real/Live-PF 1,3. `getCanonicalConnectionSettingsOverlay` liest die Legacy-Settings und überlagert sie durch `settings:connection_settings:bingx-x02`; beide Stage-Koordinatoren verwenden diesen Overlay. Abweichende alte Root-/globale Defaults sind kein Beleg für wirksame PF 0,8/1,1. PF bezeichnet hier den projektinternen PositionCost-Ratio, nicht realisierte Gross-Profit/Loss. Verlustfenster 12 aktiviert, native Trend-Mehrfachmatrix aktiviert, positionCost 0,1 %, Volumenfaktoren 1.

Nach Ende des laufenden Tests: Abschlussbericht/Abrechnung/Cleanup und gegebenenfalls Halt-Audit auswerten, anschließend alle drei Dienste und erlaubte Health/Status/Logs/Settings/Live-Summary-Endpunkte prüfen. `acceptance-pr333-installed.json` ist wegen zeitgleichem Wartungsbeginn kein gültiger API-Abnahmenachweis. Den vom Browser ausdrücklich blockierten Statistik-Pfad nicht erneut oder über anderen Transport abrufen. HTML mit echten endgültigen Messwerten und diese Fortsetzung veröffentlichen; Produktionsreife und neue unabhängige 14-Tage-Profitabilität bleiben ohne die fehlenden Nachweise offen.


### VST25 r2 beendet; konkrete FIL-Rundungskorrektur — 2026-09-09 02:38 UTC

Der r2-Lauf endete 02:28:24 UTC nach 988,762s mit Exit1: neun vollständig bestandene Zyklen, zehnter FIL-Zyklus mit gefülltem Entry/Add/Close, insgesamt 81 Venue-POST-Orders inklusive eigener Fehlerbereinigung. Die frischen Snapshots funktionieren; der neue konkrete Fehler ist dezimale Mengenrundung: FIL Entry 2,4 + Add 2,4 = 4,8, aber der gemeinsame Mengenrechner reduzierte den Close durch `Math.floor((4.8 + Number.EPSILON) / 0.1)` auf 4,7. Die verbleibenden eigenen 0,1 wurden im Cleanup geschlossen. Finale eigene Restmenge/Restorders 0/0, gewählte Symbole ohne Restposition/-order. Vollständiger Cleanup-Gate trotzdem false, weil sich die fremde Baseline (anfänglich CP-Position und zwölf Orders) während des Tests geändert hatte. Das darf nicht als bestandener Gesamtlauf oder fremde Menge als eigene ausgegeben werden. Alle drei CTS-K-N-Dienste aktiv/NRestarts0; alter Entry-Halt unverändert, Empty-Book-Clear nicht ausgeführt.

Aktuelle Korrektur in `lib/order-quantity.ts`: Dezimalwerte auf Basis ihrer kanonischen Stringdarstellung ganzzahlig durch den Venue-Schritt teilen, anschließende Rückkonversion nur einmal. Kein fester absoluter EPSILON beim Close; echte Mengen unter dem Schritt werden strikt abgerundet. Entry behandelt höchstens eine relative Float-ULP über einem exakten Schritt als Rechenrest, damit DCA `0.01*1.1` nicht künstlich um einen Lot steigt. Schrittpräzision aus dezimalem Exponenten statt binärem `toFixed(18)`-Artefakt. Tests für FIL-Service-Submission, exakte Schritte von 1e-18 bis 10, echte Unterschreitung und DCA-Rechenrest. Checkpoint `/workspace/backups/CTS-K-N/20260909T023051Z-pre-fractional-close-grid-fix`. Drei gezielte Suiten/81 Tests bestanden. Finale Gesamttests, Veröffentlichung, Merge, offizieller Reinstall und erneuter Exchange-Nachweis noch offen; nur bereits gemergtes grünes main deployen.

### Order-dispatch counters and legacy-position reconciliation — 2026-09-09

Die aktuelle Prüfung bestätigte zwei getrennte Ursachen für die irreführende Fehleranzeige: Lifetime-Ledgerwerte enthalten historische Exchange-Fehler, während der aktuelle Live-Dispatch erwartete Dedup-/Parent-Fill-/Kapazitäts-/Cooldown-Entscheidungen als `rejected` zurückgab. `lib/live-dispatch-outcome.ts` klassifiziert diese Ergebnisse jetzt als `deferred`; `failedToOpen` enthält nur terminale Rejects, Fehler, fehlende Entries, fehlende Ergebnisse und unbekannte Status. Das Stats-API liefert `dispatchOutcome.deferred`; QuickStart, Active-Connection- und Statistics-Overviews zeigen Lifetime-Fehler/Rejects getrennt von aktuellen Fehlern, Blocks und Deferrals.

Die Reconcile-Ausnahme `Cannot read properties of undefined (reading 'push')` wurde beseitigt: alte Live-Positionen ohne `progression` verwenden nun den begrenzten `pushStep`-Helper. Gezielte Klassifizierungs-/Regressionstests und TypeScript sind grün. Checkpoint für diesen Arbeitsstand: `/workspace/backups/CTS-K-N/20260909T052609Z-pre-live-dispatch-fix`. Veröffentlichung über neuen grünen GitHub-PR, offizieller Reinstall und anschließende X02-Read-only-/VST-Verifikation stehen für diesen Fix noch aus; der bestehende `entry_protection_halt` bleibt bis zu einem frischen, atomaren Audit aktiv.

### Order-counter integrity correction — 2026-09-09

Die nächste Diagnose zeigte zwei Datenqualitäten: Das globale Live-Ledger hatte einen einzelnen `attempted`/`placed+failed`-Versatz durch getrennte epoch-validierte HINCRBY-Aufrufe; die alten per-symbolischen Fehlerwerte lagen höher, weil sie aus ungeschützten/stale Live-Stage-Callbacks stammen konnten. Terminale `placed`/`failed`-Buchungen verwenden nun `hincrbyProgressionBatch` mit gemeinsamer Epoch-Prüfung für Ergebnis und Attempt. Live-Stage per-symbolische Buchungen prüfen dieselbe aktuelle Engine-Epoch; veraltete Worker schreiben nicht mehr in `live_orders_by_symbol_v2`.

Stats und Server-Overview behalten das vollständige historische Ledger forensisch bei, kennzeichnen es aber ausdrücklich als Lifetime. Zusätzlich liefern sie `currentDispatch`/`orders*Current` aus frischen `strategy_detail:{connection}:live`-Zeilen, `counterIntegrity` und `orderCounterIntegrity` mit globalem/per-symbolischem Delta. Ein historischer Fehlerbestand wird dadurch nicht mehr als aktueller Fehlerburst angezeigt. Neue Regressionstests decken Batch-Paarung, stale Epoch und Overview-Felder ab; die betroffenen Tests (253 Assertions) und TypeScript waren erfolgreich. Vor dem nächsten Remote-Reinstall sind vollständige Tests, Build, GitHub-Checks und ein verifiziertes Backup erneut erforderlich. Der bestehende `entry_protection_halt` bleibt unverändert; keine Venue-Orders oder fremde Positionen werden für die Diagnose mutiert.

## 2026-09-09 — Order-Counter-Reparatur und Remote-Abnahme

PR #337 (`fix: keep live order failure counters coherent`) wurde nach grünem Dev-Preview-Smoke als Merge-Commit `49d0f0fe6e6ce2f5f701ae086f8e8fb66ce0b275` in `main` übernommen. Der offizielle Installer hat genau diesen `main`-Stand auf `/opt/cts-kn` reinstalliert; Schema 108, gemeinsames Netzwerk-Redis, 350 vollständige Traces, 305 Suites / 2.131 Tests, drei active systemd-Dienste und `NRestarts=0` wurden bestätigt. Das verifizierte Server-Rollback liegt unter `/var/backups/cts/cts-kn/20260909T064332Z`.

Die Read-only-Rekonstruktion des dauerhaften X02-Live-Positionsledgers (149 Zeilen, 135 reale, 0 simulierte) ergab vor der Reparatur `attempted=349649`, `placed=5`, `filled=6`, `failed=349645`. Mit Wartungsmarker und allen drei Handelsdiensten inaktiv wurde `scripts/rebuild-live-order-counters.mjs --connection-id bingx-x02 --apply` ausgeführt; der Redis-Sicherungsschlüssel lautet `progression:counter_rebuild_backup:bingx-x02:2026-09-09T06-58-20-279Z`. Danach sind die belegten Lifetime-Werte `attempted=4`, `placed=4`, `filled=4`, `failed=0`, `live_positions_created=5`, `live_positions_closed=5`, `live_volume_usd_total=42.17197`; `attempted = placed + failed` ist konsistent.

Das anschließende Read-only-Monitoring lief mit 11 Samples im Abstand von durchschnittlich 61,2 Sekunden (07:00:29–07:10:41 UTC). Alle Health-/Status-/Stats-/Live-Endpunkte waren HTTP 200, alle drei Dienste active ohne Neustart, Lifetime-Zähler blieben unverändert, `failedToOpen` und aktuelle `errored` blieben 0, eigene offene Orders/Positionen blieben 0. Redis stieg kontrolliert von 0,826 auf 0,948 GiB bei 3,937 GiB MaxMemory (historischer Peak 19,97 GiB); die vollständige Messreihe und Diagramme stehen in `docs/reports/20260909-order-counter-fix/report.html` und `summary.json`.

Live ist für X02 weiterhin angefordert/eingeschaltet und die Credentials-/Koordinationsprüfung ist gültig. Neue Venue-Eintritte bleiben absichtlich durch `entry_protection_halt` gesperrt, bis eine frische eigentumsgebundene Schutzprüfung einen sicheren Slot beweist; es wurden keine fremden Venue-Orders/Positionen mutiert. Die alten per-symbolischen Fehlerfelder bleiben als getrennte Forensik markiert und werden nicht als aktuelle Orderfehler gezählt.
