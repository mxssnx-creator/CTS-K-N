YªçŠx-®éÜj×¢ëiºÚ+Š§j[h‘éÜ¢éí×½ûóDèµ©hºÚn¶X§zÍKÊŠ‚ˆ
ˆİ˜]YŞHÛÛÜ™[˜]ÜˆH›ÙÜ™\ÜÚ]™Hİ˜]YŞH›İÂˆ
ˆÛÛÜ™[˜]\ÈH›ÙÜ™\ÜÚ[Ûˆœ›ÛHTÑH8¡¤ˆPRSˆ8¡¤ˆ‘PS8¡¤ˆU‘HÚ]›Ü\ˆ]˜[X][ÛˆY]šXÜÂˆ
‚ˆ
ˆ›İÎ‚ˆ
ˆKˆTÑNˆX]\šX[\ÙH]™\HÛÛ\]H[™XØ][ÛˆÛÛ™šYİ\˜][Ûˆ[™\[™[Bˆ
ˆHŞ[X›Û[™Û™ËÔÚÜ\™Xİ[Û‹ˆÛ™HÜ[ˆÙ]YË\ÜÚ][Ûˆ\Âˆ
ˆYZ]Y\ˆ^Xİ[™KÚ]H\‹[[™HÜİXÛÜÙHÛÛÛİÛ‹‚ˆ
ˆ‹ˆPRSˆ]˜[X]H˜\ÙH›İÜÈÚ]HÛÛ™šYİ\™Y›Û[™È\İÜH[™ˆ
ˆÜÚ][ÛÛÜİ\™[]]™H˜][Ë[ˆ^[™İ[™\™Ğ›ØÚËÑĞH[™ˆ
ˆÛÛ[[İ\ÈÜÚ][Û‹XÛİ[˜\šX[ÈÚ]İ][˜Ø][™È˜\ÙH[œ]‚ˆ
ˆËˆ‘PSˆ]˜[X]HHÛÛ\]H]X[YZ[™ÈXZ[ˆ[™XYÙH[™X]\šX[\ÙHHİX›Bˆ
ˆ™X[›İÈÚ]]ÈİÛˆYÙ\‹Ú[™^Y[]K‚ˆ
ˆˆU‘Nˆ]˜[X]HHÛÛ™šYİ\™Y™X[Ú[™İÈÛ˜ÙKX]\šX[\ÙHH]™H›İËˆ
ˆ[ˆZ\œ›Üˆ]\™XİHÈÚ[][][Û‹Ù^Ú[™ÙH^Xİ][Û‹‚ˆ
‚ˆ
ˆİ˜]YŞHÛİ[È[Ø^\È™\™\Ù[H[X™\ˆÙˆÑUË›İ[™]šYX[Ù]YÈÜÚ][ÛœË‚ˆ
‹Â‚š[\ÜÈ[š]™Y\ËÙ]Ù][™ÜËÙ]Ù][™ÜËÙ]™Y\ĞÛY[Hœ›ÛHÛX‹Ü™Y\ËYˆ‚š[\ÜÈÜ™X]R\ÚHœ›ÛH˜Ü\È‚š[\ÜÈÙÔ›ÙÜ™\ÜÚ[Û‘]™[Hœ›ÛHÛX‹Ù[™Ú[™K\›ÙÜ™\ÜÚ[Û‹[ÙÜÈ‚š[\ÜÈ[Z]Ø[›ÛšXØ[]™[Hœ›ÛHÛX‹Ù]™[ËÙ[Z]\ˆ‚š[\ÜÈÜÚ][Û•™\ÚÛX[˜YÙ\ˆHœ›ÛHÛX‹ÜÜÚ][Û‹]™\ÚÛ[X[˜YÙ\ˆ‚š[\ÜÈÙ]YÔÜÚ][Û“X[˜YÙ\‹˜[›ÚYHœ›ÛHÛX‹İ˜YKY[™Ú[™KÜÙ]YË\ÜÚ][Û‹[X[˜YÙ\ˆ‚š[\ÜÂˆÛÛ\XİˆØYÛÛ\Xİ[ÛÛÛ™šYËˆ\HÛÛ\Xİ[ÛÛÛ™šYËŸHœ›ÛHÛX‹ÜÙ]ËXÛÛ\Xİ[Ûˆ‚š[\ÜÈÙ]Ø[›ÛšXØ[ÛÛ›™Xİ[Û”Ù][™ÜÓİ™\›^Kİ™\›^S›Û‘[\HHœ›ÛHÛX‹ØÛÛ›™Xİ[Û‹\Ù][™ÜË[İ™\›^H‚š[\ÜÂˆ“ĞÒ×ĞÓÕS•ÓPVˆØ[İ[]P›ØÚÓZ[š[][T›Ùš]˜XİÜ‹ˆØ[İ[]P›ØÚÕ›Û[YR[˜Ü™[Y[˜][ËˆØ[İ[]P›ØÚÕ›Û[YS][\Y\‹ˆÙ]Xİ]™P›ØÚÔÙ]Ù^\ËˆÙ][˜]˜Z[X›P›ØÚÔÙ]Ù^\Ëˆ\œÙP›ØÚĞÛİ[ˆ™\ÛÛ™P›ØÚÔ›Ùš]˜XİÜ‘XÚ\Ú[Û‹ˆ™\ÛÛ™SZ\œ›Ü™YXİ]™P›ØÚĞÛİ[ŸHœ›ÛHÛX‹Ø›ØÚËXÛİ[\İ]H‚š[\ÜÂˆÙ]İ˜]YŞTÙ]ÛÜÙY™\İ[Ù^\ËˆÙ]İ˜]YŞTÙ]YÙ\˜]ÚˆÙ]İ˜]YŞTÙ]YÙ\”Û˜\ÚİˆÙ]İ˜]YŞSYÙ\•İ[ËˆÙ]İ˜]YŞTÙ]Ú[™İĞ˜]Ú\È™XYİ˜]YŞTÙ]Ú[™İĞ˜]Úˆ\Hİ˜]YŞTÙ]YÙ\”Û˜\Úİˆ\HÜÕÚ[™İÔİ]ËŸHœ›ÛHÛX‹ÜÜËZ\İÜH‚š[\ÜÈ›Ü›X[^™Tİ˜]YŞP^\ÈHœ›ÛHÛX‹Üİ˜]YŞKX^\Ë\Ù][™ÜÈ‚š[\ÜÈZ[›ÙÜ™\ÜÚ[Û”ØÛÜHHœ›ÛHÛX‹Ü›ÙÜ™\ÜÚ[Û‹\ØÛÜH‚š[\ÜÂˆÙ]ÚYÛ˜[ÛÛ™šYİ\˜][Û”\™›Ü›X[˜ÙP˜]ÚˆÙ]ÚYÛ˜[\™›Ü›X[˜ÙTİ]KˆØYÚYÛ˜[[™XØ][Û”Ù][™ÜËˆ›Ü›X[^™TÚYÛ˜[š\ÚËˆÚYÛ˜[ÛÛ™šYİ\˜][Û‘^Xİ][Û[İÙYˆÚYÛ˜[ÛÛ™šYİ\˜][Û”\™›Ü›X[˜ÙRY[]KˆÒQÓSÔT‘“Ô“PSÑWÓÓÒĞPÒËˆ\HÚYÛ˜[š\ÚËŸHœ›ÛHÛX‹ÜÚYÛ˜[Z[™XØ][Ûˆ‚š[\ÜÂˆ\ÔÚYÛ˜[[˜[ZXÕ˜Z[[™Ô›Ùš[Kˆ™\ÛÛ™TÚYÛ˜[^Xİ][Û”Ûİˆ\H˜Z[[™Ô›Ùš[KŸHœ›ÛHÛX‹ÜÚYÛ˜[]˜Z[[™È‚š[\ÜÂˆZ[ÚYÛ˜[˜YPÛÛ™šYİ\˜][ÛœËˆÚYÛ˜[ÛÛ™šYİ\˜][Û•˜Z[[™Ô›Ùš[KŸHœ›ÛHÛX‹ÜÚYÛ˜[XÛÛ™šYË[X]š^‚š[\ÜÂˆPVÔÕÔÓÔÔ×Õ×ÕRÑWÔ“Ñ’UÔUSËˆ›Ü›X[^™T›İXİ[Û”\˜Ù[YÙ\ËŸHœ›ÛHÛX‹İ˜YK\›İXİ[Û‹XÛÛ˜Xİ‚š[\ÜÂˆPRS—ÕQWÔ—ÔUS×ĞTÑKˆPRS—ÕQWÔ—ÔUS×ÓPVˆPRS—ÕQWÔÕQÑWÔ—ÑQUSËˆ‘U’SÕT×ÔÔÒUSÓ—ÓRS—Ô—ÔUSËˆXZ[•˜YT”˜][ÕÑÜ›ÜÜÓ[İ™Tİˆ[İ™TİÓXZ[•˜YT”˜][Ëˆ›Ü›X[^™SXZ[•˜YTİYÙT”˜][ËˆØØ[SXZ[•˜YTÛÛÜ™[˜]KŸHœ›ÛHÛX‹ÛXZ[‹]˜YK\›Ùš]Y˜XİÜˆ‚š[\ÜÂˆ›Ü›X[^™TÜĞÛİ[›Û[YT˜][ËˆÔ×ĞÓÕS•Õ“ÓSQWÔUS×ÑQUSˆÜĞÛİ[›Û[YT˜][ÕÔÙ]][\Y\‹ŸHœ›ÛHÛX‹ÜÜËXÛİ[]›Û[YK\˜][È‚š[\ÜÂˆXÜ]Z\™Tİ˜]YŞSY[[ÜSX\ÙKˆ™[Y]™Tİ˜]YŞSY[[ÜT™\Üİ\™KŸHœ›ÛHÛX‹Üİ˜]YŞK[Y[[ÜKYİX\™‚š[\ÜÈ[Z]™X[›İÜÑ›Ü“X]\šX[^˜][ÛˆHœ›ÛHÛX‹Üİ˜]YŞK\™X[[X]\šX[^˜][Û‹[[Z]‚š[\ÜÈÛÛ˜İ\œ™[˜ŞQœ›ÛQ[‹›Ü‘XXÚÚ]ÛÛ˜İ\œ™[˜ŞKX\Ú]ÛÛ˜İ\œ™[˜ŞHHœ›ÛHÛX‹Ø›İ[™YXÛÛ˜İ\œ™[˜ŞH‚š[\ÜÂˆÙ][[YPØ\Xš[]PÛÛ˜İ\œ™[˜ŞKˆÙ][[YPÛÛ˜İ\œ™[˜ŞT›Ùš[KŸHœ›ÛHÛX‹Ü[[YKXÛÛ˜İ\œ™[˜ŞK\›Ùš[H‚š[\ÜÂˆQUSĞTÑWÓRS—ÔÕTˆPVĞTÑWÔÕTˆRS—ĞTÑWÔÕTŸHœ›ÛHÛX‹ØÛÛœİ[È‚š[\ÜÂˆÛİ[Ü[“XZ[œ™XZÙİÛ‹ˆXZ[”Ù]\ÓÜ[“[™XYÙKŸHœ›ÛHÛX‹Üİ˜]YŞK\İYÙK\™[][ÛœÈ‚š[\ÜÂˆ›Ü›X[^™U˜YQ\™Xİ[Û‹ˆ™\ÛÛ™PÛÛœÚ\İ[˜YQ\™Xİ[Û‹ŸHœ›ÛHÛX‹İ˜YKY\™Xİ[Ûˆ‚š[\ÜÂˆÔPÒPSÓPVÔÔÒUSÓ”×ÔT—ÑT‘PÕSÓ‹ˆÔPÒPSÓPVÔÓÕ×ÕÔUSËˆÔPÒPSÓPVÕ“ÓSQWÔUSËˆØ[š]^™TÜXÚX[ÜÚ][Û”[‹ˆ\HÜXÚX[ÜÚ][Û”[‹ŸHœ›ÛHÛX‹ÜÜXÚX[\İ˜]YŞH‚š[\ÜÂˆY˜][İ˜]YŞR[™XØ][Û•˜\šX[ÛXŞKˆ›Ü›X[^™Tİ˜]YŞR[™XØ][Û•˜\šX[ÛXŞKˆİ˜]YŞR[™XØ][Û•˜\šX[[˜X›Yˆ\Hİ˜]YŞR[™XØ][Û•˜\šX[ÛXŞKŸHœ›ÛHÛX‹Üİ˜]YŞKZ[™XØ][Û‹\ÛXŞH‚š[\ÜÂˆXØÛİ[XZ[”İYÙKˆXØÛİ[™X[İYÙR[œ]ËŸHœ›ÛHÛX‹Üİ˜]YŞK\İYÙKXXØÛİ[[™È‚š[\ÜÂˆÛ\ÜÚYTİ˜]YŞQ^Xİ][Û‘˜[Z[Kˆ\Ğ[Tİ˜]YŞQ^Xİ][Û•˜\šX[[˜X›Yˆ\Ôİ˜]YŞQ^Xİ][Û‘˜[Z[Q[˜X›Yˆ\Hİ˜]YŞQ^Xİ][Û”ÛXŞKŸHœ›ÛHÛX‹Üİ˜]YŞKY^Xİ][Û‹\ÛXŞH‚‚‹ÊŠ‚ˆ
ˆ[[YHİYÙHÛ˜\ÚİÈ]\İ›İ\XØ]HHØ[›ÛšXØ[™\˜›ÜÙHÙ]Ù^Bˆ
ˆ
ÚXÚ[[[Û˜[H[˜ÛY\ÈHÛÛ\]HÛÛ™šYİ\˜][Ûˆ[™XYÙJKˆBˆ
ˆ[Y[]H™[XZ[œÈ[ˆH]]Üš]]]™H[™XØ][Û‹ØÛÛ™šYÈİÜ™H[™[‚ˆ
ˆH[‹Y›YÚÛÛÜ™[˜]ÜˆÜ˜\ÈÜ\˜][Û˜[ØXÚ\È[™\Ú›Ø\™™]šY]ÜÂˆ
ˆ\ÙH\ÈÛÛ\Ú[Û‹\™\Ú\İ[™Y™\™[˜ÙH[œİXY‚ˆ
‹Â™[˜İ[Ûˆİ˜]YŞTÙ]İÜ˜YÙT™YŠÙ]Ù^Nˆ[šÛ›İÛŠNˆİš[™ÈÂˆ™]\›ˆÜ™X]R\Ú
œÚLMˆŠBˆ\]Jİš[™ÊÙ]Ù^HÏÈˆŠJBˆ™YÙ\İ
˜˜\ÙM\›ŠBˆœÛXÙJŒŠBŸB‚˜ÛÛœİ•S•SQWÔÕQÑWÔÓTÒÕÓPVÔ“ÕÔÈHM‚‚\H[[YTİYÙTÛ˜\Úİ›İÈHÂˆ™Yˆİš[™Âˆ\™[™YÎˆİš[™Âˆ[™XØ][Û•\Nˆİš[™Âˆ\™Xİ[Ûˆİš[™Âˆ˜\šX[ˆİš[™Âˆ]™Ô›Ùš]˜XİÜˆ[X™\‚ˆ]™Ñ˜]ÙİÛ•[YNˆ[X™\‚ˆ]™ĞÛÛ™šY[˜ÙNˆ[X™\‚ˆ[PÛİ[ˆ[X™\‚ˆ^\ÕÚ[™İÜÏÎˆİ˜]YŞTÙ]È˜^\ÕÚ[™İÜÈ—Bˆ˜Z[[™ÏÎˆXÚÏ˜Z[[™Ô›Ùš[K›[ÙHˆœİ\˜][ÈˆœİÜ˜][Èˆœİ\˜][È‚ˆ™]š[İ\ÏÎˆÈÛİ[ˆ[X™\È›Ùš]˜XİÜˆ[X™\È]™Ñˆ[X™\ˆBŸB‚™[˜İ[Ûˆ›Ú™Xİ[[YTİYÙT›İÜÊÙ]Îˆ™XYÛ›Hİ˜]YŞTÙ]×JNˆ[[YTİYÙTÛ˜\Úİ›İÖ×HÂˆËÈ\È\ÈH
œ™\Ü[™Êˆ›Ú™Xİ[Û‹›İ[ˆ]˜[X][Ûˆ›İ[™\Kˆ]™\HÙ]ˆËÈ\È]˜[X]Y[™H[ÛÛ™šYÈX]š^™[XZ[œÈ[™\[™[H\œÚ\İYÂˆËÈH›İ[™Y™XY[Ù[™]™[ÈH\Ú›Ø\™ØXÚHœ›ÛH™]Z[š[™ÈBˆËÈÛÛ\]HØš™XİÜ˜\
[˜ÛY[™È™\˜›ÜÙH[™XYÙHİš[™ÜÊH]™\H[ÙK‚ˆËÈÙY\™Y™\™[˜Ù\ÈÚ[HÙ[Xİ[™ËˆÛÛœİXİ[™ÈH›Ú™Xİ[Ûˆ›İÈ\Ú\ÂˆËÈHØ[›ÛšXØ[
[™[X™\˜][H™\˜›ÜÙJHÙ]Ù^KˆÚ[™È]›Üˆ]™\BˆËÈ™Z™XİYØ[™Y]HXYHH›İ[™YÛ˜\Úİ]Ù[ˆ›ÜÜ[Û˜[ÈBˆËÈ[X]š^[™Ûİ[İ\™HX[›Ø™\È[™\ˆH\™ÙHÛÛÜ™[˜][Û‚ˆËÈ\ÜËˆÙH›İÈX]\šX[^™KÚ\ÚÛ›HH™]Z[™Y›İÜË‚ˆËÈXZ[Z[ˆHš^Y\Ú^™HZ[‹ZX\ˆH›Ü›Y\ˆ[\[Y[][Ûˆ™\ØØ[›™Y[ˆËÈMˆ™]Z[™Y›İÜÈ›Üˆ]™\HØ[™Y]H
Êˆ
ˆMŠJNÈ^]\İ]™H™X[ˆËÈÜ˜\È›İ][™[HÛÛZ[ˆ[œÈÙˆİ\Ø[™ÈÙˆ›İÜËÛÈHXYÛ›ÜİXÂˆËÈ›Ú™Xİ[Ûˆ[Û™HÛÛœİ[YYZ[[ÛœÈÙˆÛÛ\\š\ÛÛœÈÛˆ]™\HŞ[X›ÛˆBˆËÈX\™\Ù\™\ÈH^Xİ™\İLMˆÛÛ˜Xİ[ˆÊˆÙÈMŠHÚ]İ]ˆËÈÚ[™Ú[™È[Hİ˜]YŞHØ[İ[][Û‹Ü™\š[™È™Y™\™[˜ÙKÜˆ\œÚ\İ[˜ÙK‚ˆ\H˜[šÙYÙ]HÈØÛÜ™Nˆ[X™\ÈÙ\]Y[˜ÙNˆ[X™\ÈÙ]ˆİ˜]YŞTÙ]BˆÛÛœİÜˆ˜[šÙYÙ]×HH×BˆÛÛœİØÛÜ™Q›ÜˆH
Ù]ˆİ˜]YŞTÙ]
HO‚ˆ[X™\ŠÙ]˜]™Ô›Ùš]˜XİÜˆ
H
ˆLÌ
È[X™\ŠÙ]˜]™ĞÛÛ™šY[˜ÙH
H
ˆLH[X™\ŠÙ]˜]™Ñ˜]ÙİÛ•[YH
BˆÛÛœİİÙ\”˜[šÈH
Yˆ˜[šÙYÙ]šYÚˆ˜[šÙYÙ]
HO‚ˆYœØÛÜ™HšYÚœØÛÜ™H
YœØÛÜ™HOOHšYÚœØÛÜ™H	‰ˆYœÙ\]Y[˜ÙHˆšYÚœÙ\]Y[˜ÙJBˆÛÛœİÚY\H
[™^ˆ[X™\ŠHOˆÂˆÚ[H
[™^ˆ
HÂˆÛÛœİ\™[HX]™›ÛÜŠ
[™^HJHÈŠBˆYˆ
[İÙ\”˜[šÊÜÚ[™^KÜÜ\™[JJHœ™XZÂˆÖİÜÚ[™^KÜÜ\™[WHHİÜÜ\™[KÜÚ[™^WBˆ[™^H\™[ˆBˆBˆÛÛœİÚYİÛˆH
[™^ˆ[X™\ŠHOˆÂˆÚ[H
YJHÂˆÛÛœİYH[™^
ˆˆ
ÈBˆÛÛœİšYÚHY
ÈBˆ]İÙ\İH[™^ˆYˆ
YÜ›[™İ	‰ˆİÙ\”˜[šÊÜÛYKÜÛİÙ\İJJHİÙ\İHYˆYˆ
šYÚÜ›[™İ	‰ˆİÙ\”˜[šÊÜÜšYÚKÜÛİÙ\İJJHİÙ\İHšYÚˆYˆ
İÙ\İOOH[™^
H™]\›‚ˆÖİÜÚ[™^KÜÛİÙ\İWHHİÜÛİÙ\İKÜÚ[™^WBˆ[™^HİÙ\İˆBˆBˆ]Ù\]Y[˜ÙHHˆ›Üˆ
ÛÛœİÙ]ÙˆÙ]ÊHÂˆÛÛœİØÛÜ™HHØÛÜ™Q›ÜŠÙ]
BˆÛÛœİ˜[šÙYHÈØÛÜ™KÙ\]Y[˜ÙNˆÙ\]Y[˜ÙJÊËÙ]BˆYˆ
Ü›[™İ•S•SQWÔÕQÑWÔÓTÒÕÓPVÔ“ÕÔÊHÂˆÜœ\Ú
˜[šÙY
BˆÚY\
Ü›[™İHJBˆÛÛ[YBˆBˆYˆ
ØÛÜ™HHÜÌKœØÛÜ™JHÛÛ[YBˆÜÌHH˜[šÙYˆÚYİÛŠ
BˆBˆ™]\›ˆÜˆœÛÜ

YšYÚ
HOˆšYÚœØÛÜ™HHYœØÛÜ™HYœÙ\]Y[˜ÙHHšYÚœÙ\]Y[˜ÙJBˆ›X\

ÈÙ]JNˆ[[YTİYÙTÛ˜\Úİ›İÈOˆ
Âˆ™Yˆİ˜]YŞTÙ]İÜ˜YÙT™YŠÙ]œÙ]Ù^JKˆ‹‹ŠÙ]œ\™[Ù]Ù^HÈÈ\™[™Yˆİ˜]YŞTÙ]İÜ˜YÙT™YŠÙ]œ\™[Ù]Ù^JHHˆßJKˆ[™XØ][Û•\Nˆİš[™ÊÙ]š[™XØ][Û•\HˆŠKˆ\™Xİ[Ûˆİš[™ÊÙ]™\™Xİ[ÛˆˆŠKˆ˜\šX[ˆİš[™ÊÙ]˜\šX[™Y˜][ŠKˆ]™Ô›Ùš]˜XİÜˆ[X™\ŠÙ]˜]™Ô›Ùš]˜XİÜˆ
Kˆ]™Ñ˜]ÙİÛ•[YNˆ[X™\ŠÙ]˜]™Ñ˜]ÙİÛ•[YH
Kˆ]™ĞÛÛ™šY[˜ÙNˆ[X™\ŠÙ]˜]™ĞÛÛ™šY[˜ÙH
Kˆ[PÛİ[ˆ[X™\ŠÙ]™[PÛİ[
Kˆ‹‹ŠÙ]˜^\ÕÚ[™İÜÈÈÈ^\ÕÚ[™İÜÎˆÙ]˜^\ÕÚ[™İÜÈHˆßJKˆ‹‹ŠÙ]˜Z[[™Ô›Ùš[HÈÂˆ˜Z[[™ÎˆÂˆ[ÙNˆÙ]˜Z[[™Ô›Ùš[K›[ÙKˆİ\˜][ÎˆÙ]˜Z[[™Ô›Ùš[Kœİ\˜][ËˆİÜ˜][ÎˆÙ]˜Z[[™Ô›Ùš[KœİÜ˜][Ëˆİ\˜][ÎˆÙ]˜Z[[™Ô›Ùš[Kœİ\˜][ËˆKˆHˆßJKˆ‹‹ŠÙ]œ™]”ÜÈÈÂˆ™]š[İ\ÎˆÂˆÛİ[ˆ[X™\ŠÙ]œ™]”ÜË˜Ûİ[
Kˆ›Ùš]˜XİÜˆ[X™\ŠÙ]œ™]”ÜËœ›Ùš]˜XİÜˆ
Kˆ]™Ñˆ[X™\ŠÙ]œ™]”ÜË˜]™Ñ
KˆKˆHˆßJKˆJJBŸB‚‹ËÈH\È\™XİU˜YHXÚÈ]\İX]™H[YH›ÜˆÛÛ›ÛX[[™™XÛİ™\B‹ËÈ™\]Y\İÈ]™[ˆÚ[ˆHÚ[™ÛHÙ]\È^[œÚ]™HÈX]\šX[\ÙKˆ\È\ÈB‹ËÈØÚY[[™È]X[[K›İHØ\ˆ]™\HØ[™Y]H\Èİ[]˜[X]YˆB‹ËÈ›İ\‹\›İÈ]X[[H\È[›™XÙ\ÜØ\š[Hš[™H›Üˆ\ÈÚY\ˆİ˜]YŞHÛÜÚ[B‹ËÈ
È›İÜÈœ™XXÚYHYX\İ\™YÌ‹\Ş[X›Û]ˆÛÛ›Û\[™HMKˆH™\šYšYY‹ËÈ[X˜\ÚÙ]]X[[H\ÈÌˆ›İÜÎÈ\™ÙH™Y\ÈÜš]\È\™H[™\[™[H›İ[™Y‹ËÈ™[İË‚˜ÛÛœİÕUQÖWĞÓÓÔTUU‘WÖRQSÒS•T•SHX]›X^
ˆˆX]›Z[ŠˆM‹ˆ[X™\‹œ\œÙR[
›ØÙ\ÜË™[‹”ÕUQÖWĞÓÓÔTUU‘WÖRQSÑU‘T–HŒÌˆ‹L
HÌ‹ˆ
KŠB‚‹ËÈØ[™Y]KXÛİ[]X[H[Û™H\™HHÛÜˆ˜Z\›™\ÜÈÚYÛ˜[ˆHÚX\ØØ[\‚‹ËÈ›İÈØ[ˆš[š\Ú[ˆZXÜ›ÜÙXÛÛ™ËÚ[HH™Y\ËX˜XÚÙY›İÈØ[ˆ[™XYH]™B‹ËÈZY[Y˜]\˜[Kˆ[˜ÛÛ™][Û˜[HØÚY[[™ÈÙ][[YYX]HY\ˆ]™\B‹ËÈ]X[[H]H\ŞHRKÜÛØZÈÛ]Y]YHÛÛœİ[YH[ˆ[\™H\›ˆİ\Ø[™ÈÙ‚‹ËÈ[Y\È\š[™ÈÛ™H^]\İ]™HX]š^ˆÙY\HÛX[Ûİ[ÚXÚÜË]Û›B‹ËÈ^H›ÜˆHXXÜ›İ\ÚÈ[™[Ù™ˆY\ˆH›İ[™YØ[XÛØÚÈÛXÙKˆ\È™]Z[œÂ‹ËÈÛÛ\]HÛİ™\˜YÙH[™H™\ÜÛœÚ]™HÛÛ›Û[™HÚ]İ]\›š[™ÈØÚY[\‚‹ËÈÚ\›ˆ[ÈHÛZ[˜[˜\ÙKÓXZ[‹Ô™X[ÛÜİ‚˜ÛÛœİÕUQÖWĞÓÓÔTUU‘WÕSQWÔÓPÑWÓTÈHX]›X^
ˆˆX]›Z[ŠˆLˆ[X™\‹œ\œÙR[
›ØÙ\ÜË™[‹”ÕUQÖWĞÓÓÔTUU‘WÕSQWÔÓPÑWÓTÈ‹L
Hˆ
KŠB›]İ˜]YŞS\İXXÜ›İ\ÚÖZY[]H]K››İÊ
B›]İ˜]YŞSXXÜ›İ\ÚÖZY[[‘›YÚˆ›ÛZ\ÙO›ÚYˆ[H[‚‹ËÈÛÛXš[š[™ÈH^]\İ]™HÜËPÛİ[X]š^\È[™X\ˆÛÜšÈİ™\ˆH]XÚÚY\‚‹ËÈÛÛXİ[Ûˆ[ˆHš[˜[™X[›İËˆÙY\]È[™]šYX[ÛXÙ\È›İ[™Y‹ËÈÚ]İ]ZY[[™ÈÛ˜ÙH\ˆ[HØØ[\ˆÜ\˜][Ûˆ
ÚXÚÛİ[XZÙHHÛÛ‹ËÈ\İÜšXÈ\ÜÈ™YY\ÜÛHÛİÊK‚˜ÛÛœİÕUQÖWÔÔ×ĞÓÕS•ĞÓÓP’S‘WÖRQSÒS•T•SH‚‹ËÈ\™ÙH^]\İ]™H\Ú\ÈØ[ˆÛÛZ[ˆ[œÈÙˆİ\Ø[™ÈÙˆšY[ËˆÙ[™[™Â‹ËÈÛ™HÑUÚ]LÊÈ\™İ[Y[ÈXZÙ\ÈH™Y\ÈÛY[Ş[˜Ú›Û›İ\ÛH[˜ÛÙHÛ™B‹ËÈYÙHÛÛ[X[™[™™]™[ÈH›ÙHÛÜœ›ÛHÙ\š[™ÈÛÛ›ÛÕRHÛÜšË‚‹ËÈ\È\ÈH˜[œÜÜ]X[[HÛ›NÈ]™\HšY[\ÈÜš][ˆ^XİHÛ˜ÙK‚˜ÛÛœİÕUQÖWÔ‘QT×ÒTÒÕÔ’UWĞUÒÔÒV‘HHX]›X^
ˆˆX]›Z[Šˆ—Ìˆ[X™\‹œ\œÙR[
›ØÙ\ÜË™[‹”ÕUQÖWÔ‘QT×ÒTÒÕÔ’UWĞUÒÔÒV‘HŒMˆ‹L
HM‹ˆ
KŠB‚‹ËÈHš[™Ù\œš[ØXÚH\ÈH™\İYY™›Ü™XZ[XØÙ[\˜]Ü‹›İ[‚‹ËÈ^Xİ][ÛˆYÙ\‹ˆÙY\[™ÈÛ™H[H›Üˆ]™\HÛÛ\]HXZ[ˆØ\\ÚX[ˆ›İÂ‹ËÈXYHHÌ‹\Ş[X›Û›ØÙ\ÜÈ™]Z[ˆ[™™YÈÙˆZPˆ[ˆ™Y\È™Y›Ü™H]Ûİ[‹ËÈ^\™Kˆ›İ[™]\ˆŞ[X›ÛÈ[ˆ•KØØXÚHZ\ÜÈÚ[\H™XZ[ÈH^Xİ‹ËÈ]\›Z[š\İXÈ˜\šX[œ›ÛH]È˜\ÙHÙ][™\™Y›Ü™HØ[››İY™™XİÜ™\‚‹ËÈÛÜœ™Xİ™\ÜÈÜˆÛİ™\˜YÙK‚˜ÛÛœİÕUQÖWÑ’S‘ÑT”’S•Ô‘QT×ÓPVÑ’QSÈHX]›X^
ˆˆX]›Z[ŠˆÌM‹ˆ[X™\‹œ\œÙR[
›ØÙ\ÜË™[‹”ÕUQÖWÑ’S‘ÑT”’S•Ô‘QT×ÓPVÑ’QSÈŒL‹L
HWÌˆ
KŠB˜ÛÛœİÕUQÖWÑ’S‘ÑT”’S•ĞĞPÒWÕÔÑPÓÓ‘ÈHÌ‚‹ÊŠ‚ˆ
ˆ][Y\œËX[›Ø™\Ë[™™XY[Û›HTH[™\œÈ[ˆ™]ÙY[ˆÛÛ\]Bˆ
ˆÛÛÜ™[˜][Ûˆ˜]Ú\Ëˆ]ØZ]Ûˆ[ˆ[™XYK\™\ÛÛ™Y™Y\ËÛY[[ÜH›ÛZ\ÙBˆ
ˆÛ›H˜Z[œÈHZXÜ›İ\ÚÈ]Y]YNÈ]Ù\È›İ™]™[H\™ÙH^]\İ]™Bˆ
ˆ˜\ÙKÓXZ[‹Ô™X[\ÜÈœ›ÛHİ\š[™ÈHÙ\™\ˆ]™[ÛÜ‚ˆ
‹Â˜Û\ÜÈİ˜]YŞQÙ[™\˜][Û”İ\\œÙYY\œ›Üˆ^[™È\œ›ÜˆÂˆÛÛœİXİÜŠ
HÂˆİ\\Š”İ˜]YŞHÙ[™\˜][Ûˆİ\\œÙYYŠBˆ\Ë›˜[YHH”İ˜]YŞQÙ[™\˜][Û”İ\\œÙYY\œ›Üˆ‚ˆBŸB‚™[˜İ[Ûˆ\ÜÙ\İ˜]YŞQÙ[™\˜][Ûİ\œ™[
Úİ[ÛÛ[YOÎˆ

HOˆ›ÛÛX[ŠNˆ›ÚYÂˆYˆ
\Úİ[ÛÛ[YJH™]\›‚ˆ]İ\œ™[H˜[ÙBˆHÂˆİ\œ™[HÚİ[ÛÛ[YJ
HOOH˜[ÙBˆHØ]ÚÂˆİ\œ™[H˜[ÙBˆBˆYˆ
Xİ\œ™[
H›İÈ™]Èİ˜]YŞQÙ[™\˜][Û”İ\\œÙYY\œ›ÜŠ
BŸB‚˜\Ş[˜È[˜İ[ÛˆZY[İ˜]YŞTØÚY[\Šˆ›Ü˜ÙHH˜[ÙKˆÚİ[ÛÛ[YOÎˆ

HOˆ›ÛÛX[‹ŠNˆ›ÛZ\ÙO›ÚYˆÂˆËÈHØ[˜XÚÈ\ÈÙ[™\˜][Û‹\ÜXÚYšXÈ[™™XÛÜ™ÈHØ]ÚÙÈX\™X]‚ˆËÈ™]™\ˆİXÚYZ\ÜÚ[ÛˆÛØ˜[H\™Nˆİ[HÛÜšÈ]\İ˜Z[]ÈİÛˆİX\™ˆËÈ[œİXYÙˆ™Yœ™\Ú[™ÈH™]Ù\ˆİÛ™\‰ÜÈ›ÙÜ™\ÜÈ[Y\İ[\‚ˆ\ÜÙ\İ˜]YŞQÙ[™\˜][Ûİ\œ™[
Úİ[ÛÛ[YJBˆYˆ
ˆY›Ü˜ÙH	‰‚ˆ]K››İÊ
HHİ˜]YŞS\İXXÜ›İ\ÚÖZY[]ÕUQÖWĞÓÓÔTUU‘WÕSQWÔÓPÑWÓTÂˆ
HÂˆ™]\›‚ˆBˆYˆ
İ˜]YŞSXXÜ›İ\ÚÖZY[[‘›YÚ
HÂˆ]ØZ]İ˜]YŞSXXÜ›İ\ÚÖZY[[‘›YÚˆ\ÜÙ\İ˜]YŞQÙ[™\˜][Ûİ\œ™[
Úİ[ÛÛ[YJBˆ™]\›‚ˆB‚ˆİ˜]YŞSXXÜ›İ\ÚÖZY[[‘›YÚH™]È›ÛZ\ÙO›ÚYŠ
™\ÛÛ™JHOˆÂˆËÈÙ][[YYX]H™\İ[Y\ÈY\ˆHÛ\ÙKÛÈ]Y]YYX[Üİ]È[™ˆËÈ™Y\ÈKÓÈØ[˜XÚÜÈØ[ˆ[ˆ™Y›Ü™HH™^ÔH˜]ÚˆH[Y\‹[Û›BˆËÈÚZ[ˆØ[ˆÙY\™KY[\š[™ÈH[Y\œÈ\ÙH\š[™È[ˆ^]\İ]™HX]š^ˆËÈ[™İ[İ\™HÛÛ›Û™\]Y\İÈ]™[ˆİYÚ]XÚšXØ[HZY[Ë‚ˆYˆ
\[ÙˆÙ][[YYX]HOOH™[˜İ[ÛˆŠHÂˆÙ][[YYX]J™\ÛÛ™JBˆH[ÙHÂˆÙ][Y[İ]
™\ÛÛ™K
BˆBˆJK™š[˜[J

HOˆÂˆİ˜]YŞS\İXXÜ›İ\ÚÖZY[]H]K››İÊ
Bˆİ˜]YŞSXXÜ›İ\ÚÖZY[[‘›YÚH[ˆJBˆ]ØZ]İ˜]YŞSXXÜ›İ\ÚÖZY[[‘›YÚˆ\ÜÙ\İ˜]YŞQÙ[™\˜][Ûİ\œ™[
Úİ[ÛÛ[YJBŸB‚˜\Ş[˜È[˜İ[ÛˆÙ]İ˜]YŞT™XÛÜ™[˜]Ú\ÊˆÛY[ˆ[KˆÙ^Nˆİš[™Ëˆ˜[Y\Îˆ™XÛÜ™İš[™Ëİš[™Ï‹ˆÚİ[Ûõë~üÒÚ$z{-®éÜj×h.
                  posCountsVolumeRatio,
                  trailingProfile: baseDefault.trailingProfile,
                  ...(baseDefault.signalRisk && { signalRisk: baseDefault.signalRisk }),
                  ...(baseDefault.prevPos && { prevPos: baseDefault.prevPos }),
                }
                StrategyCoordinator._axisLruSet(axisLruKey, axisSet)
                axisSets.push(axisSet)
              }
          }
        }
      }
    }
    return axisSets
  }

  private variantProfiles(): Array<{
    name: "default" | "trailing" | "block" | "dca"
    gate: (ctx: PositionContext) => boolean
    configs: Array<{ size: number; leverage: number; state: string; pfBias: number; ddtBias: number }>
  }> {
    return [
      {
        name: "default",
        gate: () => true,
        configs: [
          { size: 1.0, leverage: 1, state: "new", pfBias: 1.00, ddtBias: 0  },
          { size: 1.0, leverage: 2, state: "new", pfBias: 1.05, ddtBias: 15 },
        ],
      },
      {
        name: "trailing",
        // Deprecated Main-stage profile. Kept only so old persisted
        // fingerprints remain parseable; selectActiveVariants() never emits it.
        gate: () => false,
        configs: [],
      },
      {
        name: "block",
        // â”€â”€ Block gate: setting-driven; actual block counts are completed-pos
        // overlays generated at Real stage, not open-position gates. â”€â”€â”€â”€â”€â”€â”€â”€
        //
        // The cap (`blockMaxStack`) is operator-controlled (defaults to 12).
        // Each blockCount 1..blockMaxStack is emitted independently as a
        // independent Real-stage Set over every eligible selected Set.
        gate: () => true,
        // â”€â”€ Block sub-configs â”€ size is historical coordination metadata.
        // Exchange target quantity is always calculated from the authoritative
        // general position basis as `base Ã— (1 + count Ã— ratio)`; the profile
        // size must never scale that target a second time.
        // CRITICAL FIX: Reduced from 1.5/2.0 to 1.15/1.25 to prevent slippage
        // beyond SL triggers. Larger positions were getting filled at prices
        // that immediately crossed their own SL triggers on the same tick,
        // causing immediate losses. Smaller multipliers allow fills to stay
        // within the expected SL/TP band without forced closure.
        configs: [
          { size: 1.15, leverage: 2, state: "add", pfBias: 1.08, ddtBias: 45 },
          { size: 1.25, leverage: 2, state: "add", pfBias: 1.12, ddtBias: 75 },
        ],
      },
      {
        name: "dca",
        gate: () => true,
        configs: [
          { size: 0.5, leverage: 1, state: "reduce", pfBias: 0.98, ddtBias: 20 },
          { size: 0.5, leverage: 1, state: "close",  pfBias: 0.95, ddtBias: 30 },
        ],
      },
    ]
  }

  /**
   * Deterministic fingerprint of {base Set Ã— variant Ã— position context}.
   * Drives the "IF NOT ALREADY CREATED" dedup check.
   *
   * â”€â”€ Bucket ranges (P0-3, spec-aligned) â”€ï¿½ï¿½â”€â”€â”€â”€â”€â”€â”€â”€ï¿½ï¿½â”€â”€â”€ï¿½ï¿½ï¿½ï¿½â”€â”€â”€â”€â”€â”€â”€ï¿½ï¿½â”€â”€â”€â”€â”€â”€
   * Spec ranges:
   *   - Prev Positions         1-12   (13 buckets 0-12)
   *   - Last Positions W/L     1-4    (5 buckets each 0-4)
   *   - Continuous Positions   1-8    (9 buckets 0-8)
   *
   * The previous implementation under-bucketed (Math.min(5,...) for all
   * three context dimensions), which collapsed distinct spec-level
   * contexts into the same cache entry and silently reused stale Sets.
   * Now each dimension is clamped to its spec maximum so every
   * semantically distinct context produces a distinct fingerprint.
   *
   * Coordinated-vars vs. materialised-Sets: we chose the coordinated
   * approach â€” each qualifying base Set expands only for reached windows
   * across Previous, Last, Continuous, and Pause, rather than eagerly
   * materialising the entire Cartesian product. In practice the
   * operator only visits O(20-80) of them per symbol per run. The
   * alternative (materialising Sets for every combo) would blow the
   * 250-entry cap and thrash Redis with no accuracy win.
   *
   * â”€â”€ P2-3: Closed-only contract for statistics-driven buckets â”€â”€â”€â”€â”€â”€
   * `lastWins`, `lastLosses`, `prevPosCount`, `prevLosses` below are
   * closed-only by construction (see `getPositionContext` P2-1 gate).
   * `continuousCount` is intentionally active-book based â€” Continuous
   * Positions denote confirmed currently-open positions per spec.
   */
  private variantFingerprint(
    baseSet: StrategySet,
    variant: "default" | "trailing" | "block" | "dca",
    ctx: PositionContext,
  ): string {
    const bPF = Math.round(baseSet.avgProfitFactor * 10) / 10
    const bEC = baseSet.entryCount
    // Clamp each context dimension to its spec maximum.
    // cont is live-open by spec; the other four are closed-only via
    // the P2-1 gate in getPositionContext. lastPosCount is the Pause
    // variant's primary discriminator (1..8 windows) â€” adding it to the
    // fingerprint guarantees a 3-loss / 5-loss / 8-loss pause produce
    // distinct cached Sets instead of collapsing into the same bucket.
    const cont = Math.min(8, Math.max(0, ctx.continuousCount))
    const lW   = Math.min(4,  Math.max(0, ctx.lastWins))
    const lL   = Math.min(4,  Math.max(0, ctx.lastLosses))
    const lP   = Math.min(8,  Math.max(0, ctx.lastPosCount))
    const pP   = Math.min(12, Math.max(0, ctx.prevPosCount))
    const pL   = Math.min(12, Math.max(0, ctx.prevLosses))
    // DCA is an independent adjust Set for each parent Set, not a
    // position-count Set. Do not include live/closed position-count context
    // in its fingerprint or it will be recreated/rebucketed as counts change.
    const baseRef = strategySetStorageRef(baseSet.setKey)
    if (variant === "dca") {
      return `${baseRef}#${variant}#pf=${bPF}#ec=${bEC}`
    }

    const bCtx = `c${cont}/lw${lW}/ll${lL}/lp${lP}/pp${pP}/pl${pL}`
    return `${baseRef}#${variant}#pf=${bPF}#ec=${bEC}#ctx=${bCtx}`
  }

  /**
   * Build one related Main Set from a qualifying Base Set + variant profile.
   * Returns `null` if all candidate entries are rejected by the DDT cap or
   * the Set ends up empty (shouldn't normally happen at Main thresholds).
   */
  /**
   * Build a Main variant Set from a Base Set + variant profile.
   *
   * Now `async` because the prune step delegates to the shared
   * compaction policy (cached settings hash, async resolution). The
   * cache TTL keeps this effectively synchronous in steady state.
   */
  private async buildVariantSet(
    baseSet: StrategySet,
    profile: ReturnType<StrategyCoordinator["variantProfiles"]>[number],
    metrics: EvaluationMetrics,
    ctx?: PositionContext,
  ): Promise<StrategySet | null> {
    // â”€â”€ SLIM PATH (Base-Anchored Coordination Model) â”€â”€â”€â”€â”€â”€â”€â”€ï¿½ï¿½â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
    // Previously this function allocated a full entries[] by cross-joining
    // baseSet.entries Ã— profile.configs â€” ~800 array allocations/sec and
    // ~80 000 object allocations/sec at 20-symbol live-trading scale.
    // New design: compute avgPF/DDT/Cnf as scalars; return entries: [].
    // createLiveSets (line ~3774) already handles entries.length === 0 by
    // resolving Base entries via coordIndex.base.byKey.get(parentSetKey) â€”
    // O(1), zero-copy.  The Real-stage tuner for-loop over s.entries becomes
    // a no-op; coordRec.tunedAvgPF is written from s.avgProfitFactor here.
    let sumPF = 0, sumDDT = 0, sumCnf = 0, count = 0
    const baseDDTFallback = baseSet.avgDrawdownTime || 0

    // â”€â”€ Representative surviving config (variant size/leverage coordination) â”€
    // Dispatch selects the Base `bestEntry` by max PF, so the variant's
    // coordinated sizing must come from the surviving config with the highest
    // PF bias (the one that "wins" alongside that entry). Track it here so the
    // profile `size` and variant `leverage` survive the slim path and reach
    // dispatch. Independent Block Count Sets later replace this legacy profile
    // size with the exact total multiplier `1 + count Ã— volume ratio`.
    let repConfig: { size: number; leverage: number; pfBias: number } | null = null

    // Exhaustively evaluate every Base entry Ã— profile configuration. History
    // compaction is a persistence concern and never truncates this calculation.
    for (const baseEntry of baseSet.entries) {
      for (const cfg of profile.configs) {
        const pf = scaleMainTradePfCoordinate(baseEntry.profitFactor, cfg.pfBias)
        const baseDDT = baseEntry.drawdownTime > 0 ? baseEntry.drawdownTime : baseDDTFallback
        const ddt     = baseDDT + cfg.ddtBias
        if (ddt > metrics.maxDrawdownTime) continue
        sumPF  += pf
        sumDDT += ddt
        sumCnf += Math.min(0.99, baseEntry.confidence)
        count++
        if (!repConfig || cfg.pfBias > repConfig.pfBias) {
          repConfig = { size: cfg.size, leverage: cfg.leverage, pfBias: cfg.pfBias }
        }
      }
    }

    if (count === 0) return null

    const avgPF  = sumPF  / count
    // Do not manufacture a valid variant by clamping every constituent to the
    // configured gate. The measured coordinate must clear the gate itself.
    if (avgPF < metrics.minProfitFactor) return null
    const avgDDT = sumDDT / count
    const avgCnf = sumCnf / count

    const axisWindows = profile.name === "dca"
      ? { prev: 0, last: 0, cont: 0, pause: 0 }
      : ctx
        ? {
            prev:  Math.max(0, Math.min(12, ctx.prevPosCount)),
            last:  Math.max(0, Math.min(4,  ctx.lastPosCount)),
            cont:  Math.max(0, Math.min(8,  ctx.continuousCount)),
            pause: Math.max(0, Math.min(8,  ctx.lastPosCount)),
          }
        : { prev: 0, last: 0, cont: 0, pause: 0 }

    return {
      setKey:          `${baseSet.setKey}#${profile.name}`,
      parentSetKey:    baseSet.setKey,
      variant:
        baseSet.trailingProfile && baseSet.indicationType !== "special" && profile.name === "default"
          ? "trailing"
          : profile.name,
      axisWindows,
      indicationType:  baseSet.indicationType,
      direction:       baseSet.direction,
      avgProfitFactor: avgPF,
      avgConfidence:   avgCnf,
      avgDrawdownTime: avgDDT,
      entryCount:      count,
      // EMPTY entries[] â€” Base entries resolved at dispatch via
      // coordIndex.base.byKey.get(parentSetKey).  Eliminates the primary
      // V8 heap driver (~80 000 object allocations per second).
      entries:         [],
      // Variant size/leverage coordination â€” carried as scalars so dispatch
      // applies the Adjust variant's OWN sizing (block vol-ratio-scaled,
      // dca 0.5Ã—) instead of the Base entry's 1.0Ã—/1Ã—. Trailing is a
      // Base-stage range-coordination type and flows via trailingProfile.
      // See StrategySet.
      // Scoped to NON-default variants: the `default` variant exists to
      // validate & MIRROR the Base Set, so it must keep the Base entry's own
      // size/leverage (writing the profile config here would silently change
      // default dispatch leverage). Only the additive/independent variants
      // carry their own coordinated sizing.
      ...(repConfig && profile.name !== "default" && {
        variantSizeMultiplier: repConfig.size,
        variantLeverage:       repConfig.leverage,
      }),
      // Trailing is a Base-stage range coordination profile. Preserve it on
      // every Main projection (default and adjust) so cached/recoordinated
      // Sets, axis fan-out, Real dispatch and live control-order SL anchoring
      // all resolve the exact same trailing range without relying on a later
      // mutable cache patch.
      ...(baseSet.trailingProfile && { trailingProfile: baseSet.trailingProfile }),
      ...(baseSet.signalRisk && { signalRisk: baseSet.signalRisk }),
      ...(baseSet.prevPos && { prevPos: baseSet.prevPos }),
    }
  }

  /**
   * Enforce max entries per Set using the shared threshold-compaction
   * policy (`lib/sets-compaction.ts`) in `mode: "best"`.
   *
   *   â€¢ Floor       = caller-provided `max` (so existing call sites that
   *                   compute their own per-Set max keep working).
   *   â€¢ thresholdPct= operator-controlled (Settings â†’ System â†’ Set
   *                   Compaction). Defaults to 20% per spec, so a
   *                   `max=250` floor admits up to 300 entries before
   *                   the compactor fires.
   *   â€¢ Mode "best" = stable-sort by PF desc, keep top floor, then
   *                   re-sort by timestamp asc so chronological order
   *                   is preserved downstream.
   *
   * The result is the same shape the legacy pruner returned (best-PF
   * first within the kept set) â€” but it only does the sort + slice
   * once every (ceiling - floor) calls instead of every call. Hot
   * paths that build a Set from many indications now see a meaningful
   * CPU drop on the prune step.
   *
   * `compactionThresholdPct` is read once and cached on the coordinator
   * instance â€” see `getCompactionThresholdPct`.
   */
  private async pruneEntries(entries: StrategySetEntry[], max: number): Promise<StrategySetEntry[]> {
    if (entries.length <= max) return entries
    const thresholdPct = await this.getCompactionThresholdPct()
    const cfg: CompactionConfig = { floor: max, thresholdPct }
    return compact(entries, cfg, "best", (e) => Number(e.profitFactor) || 0)
  }

  /** Cached threshold-pct lookup. 5s effective TTL via the underlying helper. */
  private _compactionThresholdPctCache: { v: number; t: number } | null = null
  private async getCompactionThresholdPct(): Promise<number> {
    const cache = this._compactionThresholdPctCache
    if (cache && Date.now() - cache.t < 5_000) return cache.v
    try {
      // Use the coordinator-entries pool key â€” it carries the operator's
      // intent for "how aggressively to keep entries within a single
      // Set". Falls back to the global threshold (20%) when nothing
      // is configured.
      const cfg = await loadCompactionConfig("coordinator.entries")
      this._compactionThresholdPctCache = { v: cfg.thresholdPct, t: Date.now() }
      return cfg.thresholdPct
    } catch {
      return 20
    }
  }

  /**
   * Log strategy progression through all stages
   */
  private async logStrategyProgression(symbol: string, results: StrategyEvaluation[]): Promise<void> {
    const summary = {
      symbol,
      stages: results.map((r) => ({
        type: r.type,
        sets: r.passedEvaluation,
        avgPF: r.avgProfitFactor.toFixed(2),
      })),
      totalLiveSets: results.find((r) => r.type === "live")?.passedEvaluation || 0,
    }

    try {
      await logProgressionEvent(this.connectionId, "strategy_flow", "info", `Strategy Sets flow: ${symbol}`, summary)
    } catch { /* non-critical */ }
  }
}
